{
  "$comment": [
    "COMMITTED DELIBERATELY, and it holds `ask` rules ONLY. See .gitignore's note and CLAUDE.md",
    "D-U5.9/D-U5.10. An ask rule grants nothing — it only restricts — so committing it does not make",
    "one developer's trust decisions everybody's, which is the argument that keeps `allow` in the",
    "still-ignored .claude/settings.local.json.",
    "",
    "The Bash(curl|wget|http|httpie *) entries are the enforcement half of D-U5.9: NO SESSION MAY SEND",
    "A WRITE REQUEST TO PROSVASIS GO. A document created through Go is transmitted to AADE and legally",
    "exists. CLAUDE.md states the intent; this file is what actually holds, because ask rules still",
    "prompt in every permission mode including bypassPermissions.",
    "",
    "Do not add an `allow` array here."
  ],
  "permissions": {
    "ask": [
      "Bash(docker compose down *)",
      "Bash(docker volume rm *)",
      "Bash(docker volume prune *)",
      "Bash(docker system prune *)",
      "Bash(dropdb *)",
      "Bash(* flyway:clean*)",
      "Bash(rm -rf *)",
      "Bash(git push --force *)",
      "Bash(git push -f *)",
      "Bash(git reset --hard *)",
      "Bash(git clean *)",
      "Bash(curl *)",
      "Bash(wget *)",
      "Bash(http *)",
      "Bash(httpie *)"
    ]
  }
}
