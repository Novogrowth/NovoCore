# NovoCore — Context Primer for Fresh Chat Sessions

*Paste or attach this at the start of a new chat when you need design clarifications while working with Claude Code, so the conversation doesn't need to be re-explained from scratch. Full exhaustive detail lives in `novocore-product-brief-v4.md` in the repo — this is the condensed version for quick reference and ongoing decisions.*

---

## What NovoCore is

An internally-owned system for Novotrade S.A. (Java Jives) replacing Manager.io, and eventually Prosvasis Go. **Not just "replace Manager"** — the real goal is unifying data across disconnected systems, automating repetitive tasks, and generating real reports. Manager replacement is the first workload, not the purpose. No regulatory/compliance filing responsibility sits with NovoCore — the external accountant handles that; Go remains the invoicing system of record until Phase 11.

## ⚠️ The document model — settled 2026-08-02, and it governs everything from R1 onward

**Read this before anything else in the primer.** These decisions came out of a design session, were
absent from every repository document until 2026-08-02, and change how several later steps are read.
The governing statement is `CLAUDE.md`, *The document model*; the identifier decision is **ADR 0016**.

- **Novocore never obtains a ΜΑΡΚ itself.** Greek law requires transmission to AADE at issuance, and
  the document receives its ΜΑΡΚ and QR code there. **Legal issuance always runs through an external
  transmission path — Prosvasis Go today, a certified Πάροχος at step 40 — and that does not change in
  any phase.** A sales document appears in Novocore only *after* it legally exists.
- **Numbers are recorded, never generated — until step 40.** No sequence, no counter, no
  allocation-at-commit. What changes at 40 is narrower than it sounds: Novocore begins allocating the
  **series number** and composing the document, transmitting via the Πάροχος instead of handing the job
  to Go. It still does not obtain the ΜΑΡΚ. Verified 2026-08-02: the database has **no non-identity
  sequences at all**, so "records, never generates" is currently true by construction.
- **Naming rule:** no operation, class, method or route may be named `issue`/`issuance`. Prose about
  the legal act is fine; identifiers are not. ⚠️ **There was exactly one violation and it is fixed** —
  `CreditNoteService.issue` → `record`, `SalesController_issue` → `SalesController_recordNote`, spec
  and generated client regenerated, 2026-08-02. **This was U1's work, not Q1's** — it came from finding
  C1 on the committed surface, not from the backend follow-up queue.
- **ΜΑΡΚ, UID, QR URL and transmission status are CORE fields** on the sales invoice (**ADR 0016**).
  Go's own internal document id stays in the adapter mapping table. The test that separates them:
  *would this survive the vendor being replaced?* A statutory identifier of our own document does; a
  vendor's row id does not.
- **Behaviour varies by myDATA type.** ΑΛΠ and ΤΠΔΑ combine sale and transport, so **stock moves**; a
  plain Τιμολόγιο is purely sales and **does not** reduce stock, and this business issues both
  routinely. ⚠️ **Known limitation:** until a dispatch document exists (18b), **stock figures are
  incomplete for a routine share of real sales**, and the state must be queryable so the gap is
  measurable.
- **Document types are SEEDED from the AADE list** — activate, deactivate, edit a description; never
  author a row or its behaviour flags. ⚠️ **The model is `VatExemptionReason`, NOT `VatClass`** — VAT
  classes are seeded *and* extensible (`POST /api/vat-classes` exists), so they are the wrong analogy.
- **`Στοιχείο Αυτοπαράδοσης`** (internal consumption; inventory → fixed assets) needs a **protected
  self-customer record** on the retail-walk-in pattern, **excluded from customer sales, revenue and
  margin reporting** since revenue is recognised at cost. **Line price comes from FIFO lot cost, not
  the price list.** VAT deductible, **not** capitalised. **The accounts are an accountant question —
  refuse rather than guess.**
- **Transformation:** correcting a mistake must transform a document into the correct series or a
  return document **in one action**, series/products/customer auto-filled, never re-keyed. Needs the Go
  adapter; R1 stores only the allowed-target reference.
- **AADE publishes no live API for codifications.** The REST API only moves documents; code lists live
  in versioned PDFs, XSD files and an Excel of permitted combinations, and they change between
  versions. Seed as versioned core data, store the spec version, and diff-check periodically —
  **alerting a human, never auto-applying**, because a self-updating list would silently change what
  already-transmitted documents claim. *(Source: the design session, checked against AADE's published
  REST method list on 2026-08-02; not verified from inside this repository.)* **The AADE services that
  genuinely are adapter-shaped are different** and belong at step 28: Basic Business Registry ΑΦΜ
  lookup, and VIES.

⚠️ **Two architecture gaps recorded at the same time, both real rather than aspirational:**

- **Architecture rule 4 has no implementation.** *A core operation never waits on an external call* —
  step 11 built an **email** outbox and nothing else. Verified 2026-08-02: every `outbox` reference in
  the backend is email, backup or attachment, and `backend/adapters` and `backend/modules` hold **zero
  Java files**. No general integration-event outbox, no idempotency keys, no replay log, no ordering
  guarantee, and **ten adapters need all four.** Roadmap step **X1**, before the first adapter.
- **Adapter ID-mapping tables have no designed lifecycle** — external ID reuse, deleted-then-recreated
  external records, two adapters disagreeing about which core record an ID resolves to. Same design
  item.

**And one correction to a widely-repeated shape:** **channel already reaches the ledger.**
`SalesChannel` is an enum, `sales_invoice.channel` is `NOT NULL` with a CHECK, and step 3 split the
Sales *and* Sales-returns accounts per channel. **R1 references channel; it must not create it.** The
open question is whether a **generic analysis-dimension mechanism on journal lines** is wanted before a
second dimension is needed — account-splitting multiplies rather than adds, and retrofitting a
dimension means restating history.

**Product categories** remain three levels deep with a product in several at once — a self-referencing
table plus a join table, **not two flat columns and not an enum**. Nothing exists, not even the schema.

## Current build status (as of this primer — 2026-08-02, the roadmap-unification session)

**Detailed, always-current status lives in `docs/PROGRESS.md`.** Read that first; this section is
the summary.

- ⚠️ **There is ONE roadmap: `docs/novocore-roadmap.md`.** `docs/novocore-frontend-roadmap.md` was
  **deleted on 2026-08-02** and merged into it — backend and frontend are one sequence because several
  steps span both. Do not recreate a second roadmap. `docs/PROJECT_STATE_SUMMARY.md` was deleted at the
  same time and for the same reason: it was a snapshot duplicating `PROGRESS.md` and the roadmap, and a
  second record that drifts is exactly what let the backend queue and the frontend roadmap disagree
  about item 3 for a week. **Regenerate a summary on demand rather than maintaining one.**
- ⚠️ **The next step is 8a.** The running order is **Q1 → 8a → 8b → R1 → R2 → F5**, and F5 is not next
  for the same reason it was not next on 2026-08-02: the backend queue was prioritised ahead of it in a
  design conversation that existed only in chat until then, while three documents still said F5. It is
  the worked example for the `CLAUDE.md` rule that a design decision gets the same close-out discipline
  as a build step.
- ✅ **Q1 is BUILT AND LIVE-VERIFIED — 2026-08-03. Four items: 4+6, 5, 1, 7.** ⚠️ **Not "fully
  closed":** item 7's regression is open and 8a closes it. **The owner's browser leg passed** on all
  four checks — the description saved on role 3, cleared to its placeholder, role 1 (OWNER) showed the
  editor disabled with the system-role reason exactly as Name does, and product creation still worked
  (item 7's boxed `serialTracked`, proved from a form rather than from a test). Items 2 and 9 were done and item 3 closed as
  stale *before* Q1 was a step, and **the credit-note rename belongs to U1, not Q1**. What landed:
  `Required` and the exception moved from `core.web` into `core-api` as **`InvalidInputException`**; the
  two retail-customer rules enforced where a caller reaches them; `NewUser`/`NewRole` guarded with
  `Required.field`; `PATCH /api/roles/{id}/description`; the duplicate `operationId` fixed, the
  generator taught to **refuse** one, and the frontend workaround deleted; the eight boolean primitives
  boxed.
- ⚠️ **Backend queue item 8 left the queue and is now steps 8a / 8b, placed after Q1 and BEFORE R1.**
  That closed the standing open decision *"should item 8 be promoted within Q1?"* — the answer was
  neither promote nor leave last. **8a** is the `@Mandatory` annotation in `core-api`, one line in the
  spec generator, and a **bidirectional ArchUnit cross-check** on the compact constructor's bytecode
  (not optional: without it the annotation is ~289 hand-applied assertions nothing verifies). **8b** is
  the client regeneration and fixture reconciliation. Never concurrent with a frontend step. **Reasons
  for the placement:** R1 adds eight tables' worth of new records that should be written with the
  enforcement in place, and every screen built afterwards multiplies 8b.
- 🎯 **8a is load-bearing, not housekeeping.** Q1's item 7 boxed the boolean primitives, which improved
  the refusal message (`"serialTracked" is required and was not supplied.`) and **removed the `required`
  declaration from the spec**, because `OpenApiSchema` marks a component required when it
  `isPrimitive()` and a boxed `Boolean` is not. **78 → 75 schemas declaring `required`, measured
  2026-08-03.** The server still refuses; `tsc` no longer does. **No screen is built inside that
  window** — F5–F8 are the steps that would send these bodies and they come after 8b. Pinned in both
  directions by `spec-hygiene.test.ts`.
- 📌 **Two new backend items were raised by Q1, and neither is a free-floating queue row.**
  **Q1-a** — spec *schema* names collide exactly as `operationId` did, seven distinct `NameRequest`
  records resolving to one schema, harmless only because they are identical today — is **folded into
  8a**, because schema naming is a generator concern and 8a already regenerates the spec; its client
  fallout lands in 8b. **Q1-b** — `VatExemptionReasonService.create` has no production caller (the
  seed is Flyway SQL; the route is GET-only) — **stays open, to decide with R1**, which settles the
  seed-only pattern.
- ⚠️ **Rebuilding the app image is now an UNCONDITIONAL precondition of handing a live leg to the
  owner** (`CLAUDE.md`, close-out). Q1's first browser attempt answered
  `404 "No static resource api/roles/3/description"` against an image built **26 hours before the
  commit** — registration, generated client and spec all agreed; only the deployed artefact did not.
  **The cause is structural, not forgetfulness:** the app image serves **no frontend**, the browser
  loads from the Vite dev server and proxies `/api` through Caddy to the container, so **the frontend
  recompiles on every save and the backend only when an image is rebuilt.** A current screen calling a
  stale API is this stack's **default** state after any backend commit. The rule is *rebuild*, not
  *compare timestamps* — the comparison is a heuristic, and the same condition that produced this
  false failure produces a **false pass** nobody re-examines. ⚠️ `build` + `up -d app`, **never
  `down -v`**.
- 📌 **The build-SHA badge is attached to F10**, with a named trigger rather than "if it recurs":
  record the git SHA into the jar, expose it, and badge a frontend/backend mismatch in the app shell.
  **Step 43 needs it regardless** — once anyone else runs NovoCore, *"which version is that customer
  on?"* is a support precondition.
- ⚖️ **Recorded, not acted on: a dedicated non-owner test account** with credentials in a gitignored
  local env file, so live legs do not need the owner. Trade-off: a working credential on disk, under a
  hard rule that it exists only on a dev stack. **Not needed for 8a** (no browser leg); **8b is the
  first step that might want one.** Owner's call.
- **`U` is a step-ID prefix**: *a session that changes documentation and governance and produces no
  production code.* **U1** is the roadmap unification and documentation reconciliation of 2026-08-02;
  **U2** (⚪ unscheduled) is the `PROGRESS.md` → `PROGRESS.md` + `HISTORY.md` split. Future
  doc/governance sessions take U3, U4 … rather than entering the F/Q/R sequence.
- ⚠️ **The deferred customer VAT class override (F2a) is a sub-item of step 18, not a leftover screen
  task.** Moved out of Phase 2 on 2026-08-02. Permission gating was the original reason and is still
  open, but the decisive one is that **Novocore recomputes VAT from the line items to compare against
  Go's document**, so an override held only here would disagree with every invoice for that customer by
  a whole VAT class — a control that manufactures false alarms. **Step 18 carries a verification item:
  does recording a sales invoice recompute VAT, or store what the source document states? Answer it
  against the running system.** Precedence between product VAT class, island reduced-rate mapping and
  customer override is an open accountant decision, **needed for the island rates either way.**
- **Measured 2026-08-03 (after Q1):** 1,377 backend tests (0 failures, 1 skipped, `mvn clean verify`
  exit 0), 308 frontend tests across 31 files, 176 API operations.

- **Setup complete:** git repo at `https://github.com/Novogrowth/NovoCore.git`, working locally at `C:\Novocore` (moved off Google Drive — Drive's virtual filesystem was silently corrupting `node_modules` installs; git/GitHub is the only cross-machine sync mechanism now, never Drive).
- **Frontend foundation built and verified:** Vite + React 19 + TypeScript, Tailwind v4 (CSS-first config), shadcn/ui (style `base-nova`), React Router + an app shell (sidebar + main + Outlet). **No longer untouched — step 16 is under way on top of it; see the step 16 bullet at the end of this list.**
- **✅ Step 15 (dummy data validation) is DONE, and it found nine real defects.** A trading quarter runs **entirely over HTTP**, all twelve universal ledger invariants pass on the database it builds — and still pass after it is dumped, restored into a fresh database and swept again. That is the assertion the step exists for: not "every request returned 2xx" but *the books this API produced are correct*. **1152 tests, 0 skipped, `mvn clean verify` exit 0. Route coverage 128/133 driven and 5 excused in writing**, asserted by `assertEveryRouteCoveredExcept` rather than reported. Migration **V26**.
  - **The first six**, each committed separately: VAT rates crossing the wire as JSON numbers (fixed by a **`Rate` value type**); the freight `basis` doing the same; **AR disagreeing with the open items**, which ADR 0009 says is impossible, from three separate causes; `GET /api/open-items` omitting customer credits; **seventeen parameter-guidance messages across nine controllers being discarded**; and Spring's own refusals returning a different body shape entirely. ⚠️ Fixing the last one broke step 14's most load-bearing message (an amount must be a JSON string) until `WebExceptionHandler` was ordered ahead of Boot's advice.
  - **Three more, from 15b's remaining work.** **Sixteen routes answered `500` to a missing required field** — every reversal route among them — in the one body shape on the surface that is not RFC 7807; fixed at the root by **`Required`**, declared in the request record's compact constructor, so one statement covers six reversal routes. **A reversed freight allocation reported `0.00`** for what it took back out, so any period report summing freight variance overstated by every reversal in it. And **the whole email slice answered `400 "Bad request."` for an id that names nothing** where the rest of the surface answers `404`.
  - **None of the nine was reachable from the service layer** — `WholeScenarioIT` never crosses the HTTP boundary.
- **🛡️ "A client's mistake raised as a programming error" is now a named anti-pattern in `CLAUDE.md`, with three guards.** Defects 5, 7 and 9 are three occurrences of one root pattern inside a single step — an exception type meaning *our* code is wrong used to tell a caller that *their* request is wrong, after which the message is correctly discarded and the caller gets `400 "Bad request."` or a `500`. Same shape as proxy self-invocation, which earned its rules after biting across steps 11–12. The guards: an **ArchUnit rule** (no `..core.web..` class may construct `IllegalArgumentException`, proven to fail against a probe); **`noRouteRefusesWithoutSayingWhy`**, which sweeps every route and **found three of defect 9's four sites in the service layer, where the ArchUnit rule structurally cannot look**; and **`noRouteFailsOnAnEmptyBody`**, which forbids a `5xx` to a missing field whatever raised it. **`Objects.requireNonNull` is deliberately not banned** — it is correct on our own arguments and no static rule can tell a caller's omission from a programmer's, which is exactly the cries-wolf rule the self-invocation work avoided.
- **The permission model is now asserted rather than assumed, across the whole surface.** `PermissionSweepIT` states its expectation **independently**, in a route-prefix→`Section` table rather than read back off each handler's own `@Requires` — a sweep that derives its expectation from the declaration proves only that the interceptor applies it. Three roles: the real Remote/Order Staff (reaches **20** of 133, refused on **113**, every refusal checked to leak neither section nor role nor level); a **view-everywhere** role, which every state-changing route must refuse (catches a mutating handler declared `VIEW`); and a **granted-everywhere** role by stored grants rather than the `fullAccess` flag, which every read must admit (catches a route guarded by a section no grant can satisfy). This is the first thing in the repository to test the permission model on more than four routes.
- **✅ Step 16a is DONE — the four backend prerequisites the frontend needed first.** Raised before any
  foundation work, proposed in `docs/step-16-backend-prerequisites-proposal.md`, approved item by item,
  and **none of them touched `/frontend/`**. **1188 tests, 0 skipped, `mvn clean verify` exit 0; 137
  routes**; migration **V27**.
  - **`GET /api/me`** returns identity, role, **every** section with its level *and* `isAvailable()`
    (so a UI can tell "you may not see this" from "not built yet"), restricted fields, and language.
    Its one exception is written down rather than carved out: **`@AuthenticatedOnly`** makes "exactly
    one of `@Requires` or `@AuthenticatedOnly`" the rule, and an ArchUnit test asserts the set of
    controllers allowed to use it **in both directions** — proven against probes.
  - **Preview endpoints** for sales invoices and credit notes — **an extraction of the existing
    pricing phase, not a second implementation**, asserted by driving one request through both paths
    and comparing every figure, and proven to fail against a preview that drifts by a cent. Never
    post-then-rollback: audit entries are `REQUIRES_NEW` and survive a rollback, so that would record
    documents that never existed.
  - **The OpenAPI spec is ours, and springdoc's output is kept as the reason.** springdoc pulls
    Jackson 2 while our serialisers are Jackson 3, so it described `Money.amount` as a **number** and
    `Quantity` as an object with four boolean fields. `OpenApiSpecIT` walks the same handler mapping
    `RouteCoverage` reads — **137 operations, 150 schemas**, committed, with the build failing on
    drift and a **separate** test asserting no schema anywhere is typed `number`.
  - **Paging: the contract is settled and proven on sales invoices.** `PageRequest`/`PageResponse` in
    `core-api` (not Spring Data's, ADR 0003), offset with a total rather than a cursor, and
    `ListResponse` gaining an *optional* `page` — the spec diff was 152 insertions and **zero
    deletions**. Every ordering ends with the id, because a sort on a tied column lets successive
    pages repeat one row and skip another.
  - **⚠️ Remaining: tier-A paging on five more services** (purchase invoices, goods receipts,
    settlements, inventory lots/consumptions, the outbox). **Mechanical — same shape, no new design.**
    The recipe and the two traps are in `PROGRESS.md`.
- **✅ Step 16b is DONE — the three sections that had no HTTP surface at all.** `USERS_AND_ROLES` and
  `JOURNAL` had zero routes and Settings had neither UI nor API, so all three were direct SQL and
  would have become permanent frontend placeholders. **1326 tests, 0 skipped, `mvn clean verify`
  exit 0; 174 routes** (137 + 37). **No migration** — checked against the three CHECK constraints
  that could have forced one rather than assumed, after step 14's plan said "no migrations expected"
  and was wrong.
  - **Users & roles — 18 routes**, governed by `Section.USERS_AND_ROLES` rather than hard-coded to
    Owner/Admin, so brief §7's "multiple custom roles from the start" stays true for the section that
    administers roles. Includes `GET /api/sections`, the catalogue a role editor renders from.
  - **🛡️ Narrowing a role now ends its holders' sessions, and did not before.** `UserSessions` was
    wired into `UserService.deactivate`/`changeRole` only; **`RoleServiceImpl` had no reference to it
    at all**. Latent while role editing was direct SQL — an `UPDATE` to `role_section_grant` never
    went through Java — and not latent the moment `PUT /api/roles/{id}/grants/{section}` exists.
    **The asymmetry is asserted in both directions**: revoke, downgrade, restrict a field and reset a
    password evict; widen, re-grant the same level, re-restrict, rename and change language do not.
    Each eviction assertion was **confirmed to fail with its call removed**. `AccessLevel.isNarrowerThan`
    uses an explicit `rank()` switch rather than `ordinal()`, because the thing reading it decides
    whether a revoked user stays logged in.
  - **🛡️ User administration was a route to unlimited access; it is closed by two rules, because
    neither closes it alone.** *Per-section* on `RoleService.grant` (you may only confer a level you
    hold, with a full-access actor holding `FULL` everywhere by construction rather than by grant
    rows), and *by the `fullAccess` flag* on `UserService.create`/`changeRole` (you may not put anyone
    into a full-access role without holding one), plus a refusal to edit your own role or change your
    own. **`PrivilegeEscalationIT` walks the compound path** — build a second role, grant it
    `JOURNAL`, put an account in it — and asserts it is cut **at the grant, not downstream**, so no
    dangerous role is left sitting in the database. It was **confirmed to fail against the flag-only
    version**, which the sequence sidesteps entirely because it never touches a full-access role.
  - **Journal — 3 routes**: a paged listing (date range, account, source, sub-ledger; all optional),
    the single entry with its lines, and the account ledger. `/api/accounts/{id}/ledger` is governed
    by **`JOURNAL`, not `CHART_OF_ACCOUNTS`**, despite the path — asserted behaviourally, and carried
    as an explicit exception in `PermissionSweepIT`'s table. **`source` accepts all ten
    `JournalSource` values**, not the six brief §6 names. **No write routes**: a manual-entry screen
    is a design conversation, not something to smuggle in behind a listing.
  - **⚠️ `JournalEntrySummaryView` exists because `JournalEntryView` refuses to exist without its
    lines** — and *not* for the reason four javadocs originally gave. The received wisdom that a
    fetched collection forces in-memory paging (`HHH000104`) is **Hibernate 5 behaviour**; measured
    on 7, the limit is applied in SQL and collections load separately (5 entities/0 collection loads
    without a fetch, 15/5 with one). So the cost is an **N+1 per page, not an out-of-memory**. The
    test meant to protect this **passed against a deliberately introduced fetch** until it was
    rewritten against the measured figures. Recorded because a false justification is how a good
    decision gets reversed by whoever checks it.
  - **Filtering uses Specifications, not one JPQL string with optional predicates**: PostgreSQL
    cannot infer a type for a bare parameter in `? IS NULL` and refuses the statement outright. Casts
    would have worked and left five dead conditions in every plan.
  - **Settings — 3 routes, and it is an allowlist rather than a view of the table.** `SettingsCatalog`
    (18 entries) binds to `{key}`, so an uncatalogued key **has no route**: the whole `backup.*`
    namespace is excluded structurally. **Per-key exclusion would not have been enough** —
    `backup.drive.*.folder-id` and `.client-id` are *not* flagged secret and would arrive in the
    clear from any `listRedacted()`-based listing. Values are **validated before they are stored**, so
    a refused write leaves the previous one intact; today `SettingsService.put` accepts `"0,03"` and
    the failure surfaces on the next invoice recorded. **`cash.payment.limit` is readable and never
    writable** — statutory, not immutable. **Six guards, three proven to fire against probes**,
    including an ArchUnit rule keeping `..core.web..` off the untyped `SettingsService`.
  - **VAT classes and units of measure got write routes under their existing sections** (13 routes),
    not `SETTINGS` — asserted behaviourally, since a role with `SETTINGS:FULL` reaches neither.
    **There is deliberately no route to change a rate**, and its absence is asserted: editing one
    would retroactively change what every invoice already issued under that class appears to have
    charged, so a rate change is a new class plus a deactivation.
    `GET /api/units-of-measure/without-mydata-code` makes **Q38's outstanding list visible for the
    first time** — it was answerable only from `psql`.
  - **🐛 One defect fixed that was not this step's**: `MethodArgumentTypeMismatchException` is an
    `IllegalArgumentException`, so an **unparseable enum answered a bare `400 "Bad request."`** —
    `CLAUDE.md`'s named residual, the wrong-but-non-empty value none of the three guards can see.
    Found by `PermissionSweepIT` on the first enum-typed *path* variables and **latent on every enum
    query parameter since 16a**. Fixed in `WebExceptionHandler` for the whole surface.
- **🛡️ A permission change now takes effect immediately (session eviction).** Found while building
  `/api/me`: the principal is a snapshot taken at login, so **deactivating an account did not log it
  out** — for up to a session's lifetime, with no indication to the operator. Eviction rather than a
  short-lived cache, because a window is exactly what must not exist when cutting off a departing
  employee. `UserSessions` in `core-api`, `NovoCoreSessionRegistry` in `app`, called by `deactivate`
  and `changeRole` **inside their own transaction**. **Keyed by user id, not by the principal object**
  — Spring Security's own registry keys by principal, and ours wraps a `UserView` whose display name,
  language and role all change, which would orphan the sessions and make eviction *report success
  while ending nothing*. `UserSessions` is a **required** constructor argument, so the app refuses to
  start without one rather than silently evicting nobody. Proven over real HTTP, four of five tests
  confirmed to fail with the eviction removed. ⚠️ Residuals, both deliberate: the registry is
  **per-process and in memory** (one instance, as the deployment already assumes), and a change that
  does *not* warrant ending a session — a rename, a widened grant — still applies at next login.
- **Backend Phase 1: steps 0–15 done (plus inserted steps 3b, 4b, 16a and 16b).** Step 12 is operationally commissioned as well as built, step 13 — the test suite consolidation sweep — found and fixed one real ledger defect (Q45 → ADR 0015), **step 14 built the REST surface: 133 routes, migration V25, and Q44 answered in full**, and **step 15 validated it**. The agreed order was 15 validation → 16 frontend, and **that premise is now settled by evidence rather than argued:** when step 15 began, roughly half the 133 routes had never received an HTTP request and the untested half was the half that moves money. Nine defects later — none of them reachable from the service layer — the case is made. **Step 16, the frontend, is now under way** — with its four backend prerequisites done (step 16a) *and* the three sections that had no API at all now built (step 16b), so no screen has to ship as a permanent placeholder. See the step 16 bullet below. **The only backend work left before it is finishing tier-A paging on five more services, which is mechanical.** PLB-1 (2FA) stays deferred — its trigger, external or remote access, has not arrived.
  - Step 1 — Maven multi-module skeleton (`core-api` / `core` / `adapters` / `modules` / `app` / `architecture-tests`), ArchUnit guardrails, Docker Compose with Caddy HTTPS, GitHub Actions CI.
  - Step 2 — `Money` / `Quantity` / `SubLedgerRef`, schema conventions, migrations V1–V3, `SettingsService`, `AuditLogService`, `AttachmentService`, audit columns.
  - Step 3 — `AccountGroup` / `Account`, seven types, four kinds, `AccountSystemKey`, `ChartOfAccountsService`, migration V4 seeding **65 accounts across 13 groups**, plus `SchemaConventionsIT`.
  - Step 3b — `VatClass` (9 real Prosvasis Go classes seeded), `VatClassPrecedence`, `VatExemptionReason` (structure only, unseeded), `ChargeType` (structure only, unseeded), migration V5.
  - Step 4 — users, roles, two-layer permissions (migration V6), Spring Security session auth, first-owner bootstrap, `auditorAware` wired to the real user.
  - Step 4b — the first REST endpoint, `GET /api/chart-of-accounts`, read-only, existing to make the `..core.web..` ArchUnit boundary real.
  - **V7 and V8** — the two items queued at the last close-out: `Delivery income` / `COD fee income` accounts with the two `ChargeType` rows seeded against them (Q27), and **the real 29-row AADE VAT exemption reason seed** from Prosvasis Go.
  - Step 5 — `Product`, `Customer`, `Supplier`, `Asset` (migration V9). Q5, Q8, Q9 and Q12 all answered and built. Two new sections (`SUPPLIERS`, `FIXED_ASSETS`), and `PRODUCTS`/`CUSTOMERS` are no longer reserved.
  - **V10** — the VAT rate bound's blind spot closed. V5's 0–100 CHECK claimed to catch a rate written as a fraction and did not (`0.24` is inside 0–100). Now **exactly 0, or 1–100**, since the 0% class is real. Same defect as step 5's depreciation rate; **any future rate column should be checked for it.**
  - **V11** — `UnitOfMeasure` converted from a Java enum to a runtime-editable table (Q34), because myDATA's unit codes are AADE's data and an enum constant cannot own them.
  - Step 6 — `InventoryLot`, `SerializedUnit`, `StockLocation`, the stock-per-location and sellable-stock queries, the `UnitCost` type, the `WriteOffReason` enum, and full bundle/composite support (migrations V12–V13). Q7, Q25 and Q11 all answered and built. New section `INVENTORY`.
  - Step 7 — the journal engine: `JournalEntry`/`JournalLine`, debits=credits as a **deferred constraint trigger**, balances and a trial balance computed on read, reversal and in-place amendment per Q13, the Output/Input VAT split per Q14, and the inventory write-off that discharges the step 3 and step 6 obligation (migrations V14–V15, ADRs 0006 and 0007). Q13, Q14, Q19, Q26, Q15 and Q16 all answered and built. New section `JOURNAL`.
  - Step 8 — purchasing and consumption: `PurchaseInvoice`, `GoodsReceipt`, `GrIrMatch`, `StockConsumption`, GR/IR clearing in both directions, the **purchase price variance** account, FIFO consumption with the negative-stock flag, and the lot's source-document reference (migration V16, **ADR 0008**). ADR 0004's open item, Q17 and Q39 all answered and built. New section `PURCHASING`.
  - Step 9 — the sales side and the open-item layer: `SalesInvoice`, `CreditNote`, `Settlement` (Receipt and Payment in one table), `OpenItemAllocation`, `CustomerCredit`, `BankTransfer`, plus serialized selling, stock returns and the seeded retail customer (migration V17, **ADR 0009**). **Q10, Q15's remainder, Q16 and Q26 answered and built; Q31 confirmed with no change; all seven recorded obligations discharged.** New sections `SALES` and `SETTLEMENTS`, and 13 new `AccountSystemKey` values.
  - Step 10 — freight / landed cost allocation: `FreightAllocation`, the `Landed cost variance` account, a lot's cost split into a frozen **received** half and an allocated one, and the landed-cost catch-up on returned stock (migrations V18–V19, **ADR 0010** and **ADR 0011**). **Q18 answered — the last question blocking a numbered step — and step 6's last-purchase-price obligation discharged.** `ProportionalAllocation` moved to `shared`. Two commits, deliberately: the second is a defect found by *reviewing* the first.
  - Step 11 — the **shared email service**: `EmailSender`, an **outbox table** written in the caller's transaction, a scheduled dispatcher, exponential retry, and the **Reply-To header on every message** (migration V20). Both configuration decisions were answered before the code: all email configuration lives in **Settings**, and the sending address `erp@novotrade.gr` is unmonitored so **`smtp.reply-to` is `kostas@novotrade.gr` and required**. **The password is the one setting no migration seeds** — it arrives once via `NOVOCORE_SMTP_PASSWORD`, because a credential in git is in git permanently. Two defects found by tests rather than by reasoning, both recorded — and a **third found by reviewing the fix for the second**: a row already at its attempt limit would violate a CHECK at *flush*, outside any `try`, rolling back the whole claim transaction and stalling all email exactly as before. **Guarded by prevention, since a failed flush cannot be recovered per message.** One residual is stated rather than hidden: a row that cannot be *loaded* as an entity still aborts the batch, so **adding a value to `EmailStatus` or `EmailBodyFormat` makes a downgrade unsafe.** **Q43 and Q44 are new**, neither blocking.
  - Step 12 — **automated backups (V23, ADR 0013)**: a scheduled AES-256-GCM-encrypted `pg_dump`, two Google Drive destinations, retention, and a **restore check that actually restores** — closing brief §13's long-standing "restore untested" risk. **The encryption key is an environment variable and cannot be a setting**, because the `setting` table is inside the dump; a key kept there would be encrypted inside the artefact it decrypts. **⚠️ It must therefore be recorded in a password manager** or losing the host loses the database and every backup together. **Off-site is reported separately from success** — a dump that reached no destination is not a failure and not a success either. **858 tests, `mvn clean verify` exit 0.** ~~Never run against real Drive; the image has not been rebuilt.~~ **Both done on 2026-07-29** — see the commissioning entry above.
  - **Step 11, revisited (V21, ADR 0012)** — **an emailed document is referenced, not copied.** V20 gave `email_outbox_attachment` its own `content bytea NOT NULL` and defended it; the defence covered generated PDFs and reports, and missed the case that costs most — a file that is *also* an `AttachmentService` record then sat in two tables and in every backup. Now **two shapes, exactly one per row**, enforced in Java *and* by CHECK: a reference carrying no bytes, or inline bytes for a file that exists nowhere else. **Viewing a sent attachment is one call for either shape**; **a deleted document degrades gracefully** (`ON DELETE SET NULL`, the entry still names the file and says why it is gone) and **one deleted before dispatch fails that message alone, loudly**. **This is what splits Q43 in two** — see below. **815 tests, `mvn clean verify` exit 0.**
  - Step 13 — the **test suite consolidation sweep (ADR 0014)**: a property-based testing harness written in-repo, properties over `Money`/`Quantity`/`UnitCost`/`ProportionalAllocation`, FIFO properties over twenty generated *histories* against a real database, and **`WholeScenarioIT`** — one trading year played end to end, then every invariant swept at once, ending with a real backup and restore. **jqwik could not be used, for the third time in a row and for the same reason** as `archunit-junit5` (ADR 0002) and `greenmail-junit5`: it is a JUnit Platform 1.x engine and Boot 4 brings JUnit 6. Verified against Maven Central, not assumed. **The seed is fixed by default** so a red build always means a defect; breadth comes from edge-biased generators, and exploring with `-Dnovocore.property.seed=` is a deliberate act whose findings belong in named tests. **The harness is itself proven to fail** (`PropertyTest`), and writing that proof immediately caught a shrinker that gave up after one step. **952 tests at that point, `mvn clean verify` exit 0** — 960 once Q45 was fixed.
  - **🐛 Step 13 found a real defect, and it was approved and fixed the same day: Q45 → ADR 0015.** A lot whose unit cost was not a whole number of cents **left a permanent residue in the Inventory control account once fully consumed** — measured at **−0.11 EUR on a 22-unit lot at 12.505**, and −0.01 on a three-unit lot at 10.666667, the shape every landed-cost-allocated lot has. The Goods Receipt rounded the whole delivery once; each consumption rounded its own line; the two did not add up, and `HALF_UP` made the drift systematically negative with no document behind it. **Now every posting that moves a lot puts the change in the lot's carrying value on the Inventory line**, so `remainingValue()` and the account agree by construction and an emptied lot leaves nothing. The FIFO properties that found it now run **unrestricted** over the costs that broke them, and `LotCarryingValueIT` holds the worked examples and was **proven to fail against the old formula** (five of eight red, one at 275.22 against 275.11).
  - **1152 tests passing at step 15, 0 skipped, `mvn clean verify` exit 0** *(historical — the current figure is 1,376, at the top of this file)* — 866 before step 13 (which added 94), 960 before step 14 (which added 79), 1039 before step 15 (which added 113). The figure understates step 15 twice over: `WholeScenarioIT`'s 21 invariant tests were *replaced* by 12 shared ones fed to a `@TestFactory`, and several of the new "tests" are sweeps over all 133 routes reported as one. **0 skipped is new** — the PostgreSQL 17 client tools are now installed on this machine, so `BackupIT` and both backup legs run locally as well as on CI. Compose stack verified healthy over HTTPS — and, new in step 11, **an actual login now works against it end to end** (401 → 204 → 200), the first Owner account (`kostas`) exists, and **two real emails have been sent** through `mail.novotrade.gr`.
- **🚧 Step 16 (the frontend), its foundations phase — four commits, 144 frontend tests at that point.** *(Current status is further down: F0–F3, S1 and S2 done, F4 next, 269 tests. This bullet and the two after it are the chronology that got there, kept because each records a defect worth not repeating.)* `94e17cd` foundations (the nine mechanisms every screen is built out of), `56e3726` **Products, the first real screen**, `28c4119` two guards, and a brand pass on 2026-07-31. **The defect worth knowing about:** `query: { useQuery: true }` in `orval.config.ts` turned *every* generated operation into a `useQuery` — all 92 non-GET routes included — so **merely rendering a write hook would have sent the PATCH on mount**, and again on every refetch and window focus. Nothing caught it because nothing had yet *consumed* a write hook. Now guarded in two layers (`src/api/client-shape.test.ts` over all 174 operations; `expectNoWrites()` as the standing assertion in every screen test), both proven to go red. **Same lesson as Q45 and step 15 — a checker only covers what it is pointed at.**
- **🐛 A re-render loop in `DataTable` wedged the whole tab, and it had been latent since `DataTable` was written** (`3458ee6`, 2026-07-31 — a bugfix pass between F0 and F1, **not** a roadmap step; **162 frontend tests** now). Changing any list filter left the query with no data, `unwrapList` answered that with a **freshly allocated `[]`**, `useReactTable` rebuilt its row model on that new identity and called `_autoResetPageIndex()`, which reached `setPage(0)` on a table already on page 0 — and the setter allocated a new state object anyway, so React could not bail out. **React flushes that in a microtask, so the event loop never runs again**: the response that would have ended it can never arrive, and every click after it is discarded. It was reported as *"Firefox says the page is slowing down"* plus three dead interactions, and the three were a consequence, not separate bugs. **Row count is irrelevant** — an empty response wedges identically, proven by rewriting the response — so F0 did not cause it, it just gave someone a reason to use a filter. Fixed at both ends (a shared `NO_ROWS`; setters that return the same object for a no-op). ⚠️ **The regression test needed a 50 ms response delay to fail at all** — answered instantly, msw resolves inside the same microtask the reset is queued on, and the defect measures 3 renders instead of 84. **Same lesson as the write-hooks defect above: a checker only covers what it is pointed at, and here it had to be pointed at the timing too.** Two real defects it was hiding: deactivate/reactivate **rendered nothing at all** for a `422` carrying a complete explanation, and every `Select` showed the raw value (`Unit: 4`, `Type: GOODS`, `en`) because Base UI's `Select.Value` falls back to `String(value)` without the root's `items`. **Row double-click was reported as broken and had never been built** — left unbuilt on purpose, pending a design decision that belongs to every list screen.
- **🐛 Creating a product was broken for every user, and the spec is why.** `POST /api/products` answered `400 "Malformed request body: Cannot map null into type boolean"` to the body the create form built. `NewProduct` is a Java **record**, so Jackson deserialises through the canonical constructor and hands an **absent** creator property in as `null`, which `FAIL_ON_NULL_FOR_PRIMITIVES` refuses — **so an omitted primitive and an explicit null are the same thing**, and nothing reaches a handler that could name the field. **The spec declares no required fields on any request body**: of 185 schemas exactly two — `Money` and `UnitCost` — declare one, across 71 operations that take a body. So the generated type said `serialTracked?: boolean` and the frontend was written correctly against a contract that was wrong. **✅ Fixed on the backend 2026-08-01** (item 2's primitive half — see the bullet above): the generated type is now `serialTracked: boolean`, so omitting it is a compile error, and `product-create.tsx` sends it as ordinary correct code rather than as a workaround. ⚠️ **The verification that first cleared this form stubbed the POST with a fabricated `201`** and so verified nothing; the owner reproduced the failure in a minute. **When the question is "will the backend accept this", the backend has to answer** — and where a write must not persist, take the refusal from the server (an existing SKU gives `422 "already exists"` *only if the body parsed*, which separates a parser rejection from a domain one and creates nothing).
- **🎨 The product name is `Novocore`, not `NovoCore`** (capital N, rest lowercase), corrected across all seven user-facing strings on 2026-07-31. **This is display text only** — the Java package `gr.novotrade.novocore`, the repo, every internal identifier, `NOVOCORE_SITE_ADDRESS`, the `x-novocore-*` spec extensions and the OpenAPI spec title are all unchanged, and the 414 generated-client files still carry `* NovoCore API` because that string comes from the spec and is rewritten by every regeneration. The wordmark (`frontend/public/Logo-Novocore.svg`) is in the sidebar header and on the login card; `Favicon-Novocore.png` is the favicon. **⚠️ Both supplied "SVGs" are a base64 PNG in an `<svg>` wrapper with zero `<path>` elements** — not scalable, not recolourable by CSS. Contrast is fine today (20.1:1 / 12.1:1 against the sidebar, measured) but **the logo would be invisible if dark mode were ever enabled** — `.dark` tokens exist in `src/index.css` and nothing applies the class, so whoever switches it on owns a `dark:invert` or a light-ink variant.
- **✅ F0 is DONE — the development database now holds real data, and the reason it did not is worth carrying.** Frontend work is tracked in **`docs/novocore-roadmap.md`**, the single unified roadmap; F1 was the first frontend screen in this project to be built against data that exists. The live Compose database held nothing but Flyway's seed because **step 15c — the seed pass, approved as part of step 15's full scope — was never written**, and `PROGRESS.md` recorded 15a and 15b as done without ever mentioning it in either direction. It was **not** wiped: `pg_sequences.last_value` was still NULL, never called, for `product`, `supplier`, `journal_entry` and four more, and a high-water mark survives `DELETE`, `TRUNCATE` and rollback. Built now (`521a601`): `LiveHttpTransport` (the second `HttpTransport`, the implementation that interface's javadoc has described since 15a) and `LiveSeedTest`, which refuses without an explicit base URL, without credentials in the environment, or against a target that already holds trading data — **all three refusals proven, not merely written**. The database now carries **8 products, 5 customers, 3 suppliers, 2 fixed assets and 48 balanced journal entries (131 lines, €20,372.46 each side, nine distinct sources)**, every name `TEST-` prefixed.
- **✅ F4 (SETTINGS) IS DONE, CLOSED OUT AND FULLY VERIFIED — including the owner's live browser check.** Three settings pages (Documents & Rounding, Email/SMTP, Retention) over one endpoint, VAT classes and units of measure as full list/detail/create screens, plus **search** (target list rows 6 and 7, migration **V30**, 4 GIN trigram indexes) and **sorting** adopted from S1 and S2. **All 22 sub-parts have verdicts; none is "still open."** **307 frontend tests, 31 files; 1376 backend tests, `mvn clean verify` exit 0; 175 routes** — F4 added a *parameter* to two list routes, not an operation, so the spec diff is 14 lines added and **0 removed**.
  - ⚠️⚠️ **THE FINDING TO CARRY: a fact established by reading, then built upon — now a named anti-pattern in `CLAUDE.md`.** The step was scoped believing an omitted primitive `NewUnitOfMeasure.fractionalQuantityAllowed` arrives **silently as `false`**, creating a unit that forbids fractional quantities with nobody having chosen that. That came from reading the compact constructor, which null-checks `code` and `name` and nothing else — a careful, correct reading. **The server answers `400`:** `FAIL_ON_NULL_FOR_PRIMITIVES` refuses an absent primitive before the handler runs, and **the guard is one layer above the file that was read.** The claim had already been written into **three files** before anything executed it, and was corrected by the *first run* of `F4WriteContractIT`. The tell is grammatical — *"there is no guard"*, *"the backend cannot catch it"* are claims about the **absence of behaviour across a system**, and no single file supports one.
  - **`F4WriteContractIT` is how F4 answered "will the backend accept this".** 15 tests, a real Boot server over real HTTP against real PostgreSQL, sending **the literal JSON the screens build** — written out as strings rather than constructed from the request records, because building one from `NewUnitOfMeasure` asks Jackson to agree with itself. This is the pattern later screen steps should copy; a screen test over `msw` proves wiring, never contract.
  - **Second finding: `SettingType`'s javadoc named a transport-security constant that does not exist** — it said `TLS`; `EmailTransportSecurity` has `IMPLICIT_TLS` (port 465, what the live stack runs). A setting's value is an **opaque string in the spec**, so no enum is generated, `enum-labels.test.ts` cannot see it, and the frontend must mirror the list by hand — from the prose, if nobody checks. Fixed and pinned from both ends.
  - **Three preconditions the step was scoped from were wrong, and were corrected before any code was written.** ⚠️ **VAT classes were never "add and deactivate only"** — seven routes exist, including `PATCH …/description` and the `reduced-counterpart` pair; the **rate and the code** genuinely have none and never will, and there is **no ΕΛΠ/myDATA field on a VAT class at all** (the `code` *is* the Prosvasis Go code). ⚠️ **The island reduced rates were already seeded**, since **V5** — 9 classes carrying the ΑΑΔΕ codes with the counterpart chain already populated (24→17, 13→9, 6→4 *to `1041`, the αρ.31 variant*, 4→3) — where `PROGRESS.md` claimed none was. **Applicability is now decided, not open: Java Jives ships to reduced-VAT islands.** ⚠️ **And "General" had no catalogue key to put on it**, so the nav item was **dropped** rather than shipped empty.
  - ⚠️ **A field can be unavailable in FOUR ways, and only two are `FieldEditor` states** — now written up in `frontend/README.md`. Beyond `editable: false` (*not yours*) and `lockedReason` (*fixed on this record*), there is **"no route exists on any installation"** — which must render as **plain text with the reason, never a disabled control**, because a disabled control invites a hunt for the permission that unlocks it and there is none. Used by the VAT rate and code, the unit code, and `cash.payment.limit`. The fourth is **"not built yet"**, which owes an **absence test** — `reduced-counterpart` is the worked example.
  - **`mydataCode` is the first `lockedReason` that is not about one special record**: settable exactly once, then frozen, so it shows **disabled with the reason**. **`cash.payment.limit` is statutory** (€500, N. 5301/2026) and has no write route — asserted from both ends, the screen rendering no control and the real server refusing the `PUT`.
  - ⚠️ **`{key}` in `PUT /api/settings/{key}` is the ENUM CONSTANT** (`LEDGER_ROUNDING_THRESHOLD`), while the response body's own `key` field is the **dotted** spelling. Both live side by side in `pages/settings/settings-catalogue.ts`, and `settings-catalogue.test.ts` asserts that file covers `SettingsCatalog` **exactly** — a nineteenth key added on the backend otherwise lands on no page at all.
- **📌 OPEN OBLIGATION, and F4 did NOT close it — the database does not sort the way the browser does.** F4 answered the *question* (`el-GR-x-icu` was **never applied as a real Postgres collation** — measured five ways on the running stack: default still `C`/libc, zero user-created collations, zero columns with a non-default collation, zero indexes containing `COLLATE`, zero occurrences in any migration or Java file). **Recording that was the sub-part; the divergence itself is open.** Today the browser orders by `Intl.Collator('el')` and every list endpoint orders by **bytes under locale `C`** — and it does not show **only because all five endpoints return their rows whole**, so the browser re-sorts the entire list. That is a property of the data being small, not of the design. ⚠️ **The day any of these lists gains server-side paging, the disagreement becomes visible and wrong.** Closing it needs `ORDER BY … COLLATE "el-GR-x-icu"` (available on the running image, nothing to install) matched against `collation.test.ts`'s pinned PostgreSQL output, and **still no collation index**. **Not scheduled — whoever adds paging to a list screen owns it.**
- **⚖️ HELD FOR THE OWNER — does test-environment parity become a queue item with teeth?** Raised at S2's kickoff, 2026-08-01, and **deliberately parked: do not act on it in either direction.** `CLAUDE.md`'s *"a test environment configured unlike the real one"* names four knobs the real deployment sets that the test environment does **not** pin or assert — **timezone, `DateStyle`, PostgreSQL major version, and Java default locale/charset**. Locale and encoding *are* pinned and asserted; those four are the same shape and are not. The choice is between **its own backend queue item with an enforcement mechanism** (pin each, assert the pin, so removing one fails the build the way the collation pin does) and **staying a `CLAUDE.md` note**. The evidence that bears on it: S1's collation divergence was invisible to the entire suite before *and* after, and only the live check caught it. ⚠️ S2 did not touch it — though `collation.test.ts` asserts `Intl.Collator('el')` resolved to `el` rather than a root-locale fallback, which is the **Java-default-locale knob's JavaScript twin** and a fifth instance of the shape. That assertion stays whichever way the decision goes and is **not** a down payment on either option. Full framing in `PROGRESS.md`.
- **✅ S2 (SORTING) IS DONE — sortable columns on all five list screens, and the collation question is settled.** ⚠️ **The finding to carry: `ORDER BY` under `--locale=C` is byte order, and that was already live** — all five endpoints order in the database (`findAllByOrderByNameAsc`, `findAllByOrderBySkuAsc`), so `Zebra` sorted before `apple`, `Ácme` after `zebra`, and **every Greek name after every Latin one** with `Ωμέγα` ahead of `αθήνα`. On the live seed that put `Πελάτης Λιανικής` below all five `TEST-CUSTOMER-*` rows. ⚠️⚠️ **`pg_c_utf8` does NOT fix it, and it is the obvious thing to reach for** — S1's collation changes case *mapping*, not sort *order*, and its `ORDER BY` output is character-for-character identical to `C`'s (measured side by side on the running stack). **Decided 2026-08-01: `el-GR-x-icu`, Greek block first, fixed** — not `und-x-icu` and **not** following the account language, since a list that reorders when somebody switches UI language is worse than one that does not. `Intl.Collator('el')` returns **byte-identical** output and `collation.test.ts` pins PostgreSQL's literal result, so the browser half and the eventual database half cannot drift. **No collation index** — `CLAUDE.md`'s ICU rejection was about an *indexed* expression, and a plain `ORDER BY … COLLATE` is computed per query; the risk only returns with an index, where PostgreSQL records `collversion` and warns loudly. **Sorting is client-side**, and that was verified rather than assumed: the running container's own bytecode shows the five list controllers accept `active search` while Journal/Sales accept `page size sort direction`, so every row is already in the browser. ⚠️ **`DataTable` now refuses to client-sort a server-paged list** — sorting one page of many and presenting it as the order of the whole table is convincing and wrong — so a column there needs a `meta.sortKey` the endpoint declares; **none of the seven column files carries one yet (⚠️ seven since F4, not five)**, so the day one gains paging its sort controls disappear until somebody adds them. **One real defect found by a test:** TanStack chose a column's first sort direction from **row zero's value**, so the direction of a user's first click depended on which record happened to be on top. Fixed with `sortDescFirst: false`. **269 frontend tests, 27 files.** ✅ **Its browser leg is closed** — the owner ran it personally on 2026-08-01, with F4's. **Measured at 0.7 h active, 216k out.** ⚠️ **What S2 did NOT do, and F4 confirmed: `el-GR-x-icu` was never applied to the database.** See the F4 bullet.
- **✅ F1 (Suppliers), F2 (Customers) and F3 (Users & Roles) are DONE.** *(F4 is done too — see its own bullet below.)* F3's two decisions were taken before it was built: the grant grid is a **segmented three-state toggle per section** with every level the caller cannot confer **disabled and saying why**, and setting somebody else's password is **generate, show once, force an acknowledgment, never again — no confirm-field**, since the operator did not choose the value and cannot mistype it. Two shared components came out of it, both documented in `frontend/README.md`: `SegmentedControl` (which also refuses to be emptied — a grant row has no "no answer") and `PasswordHandoff` (the only place this application ever displays a credential; not a query key, not cached, not in a URL, and no route returns one). ⚠️ **The trap worth carrying:** a **full-access role holds everything with no grant rows at all**, so a grid drawn from `RoleView.sectionGrants` renders every section as `NONE` for Owner and Admin — the screen stating the opposite of the truth about the two most privileged roles. Rows come from `GET /api/sections`; the flag is checked first. **F3 was closed out on 2026-08-01 with all fifteen sub-parts verdicted done and hours measured at 0.9** (`496c7be`→`aea0e56`, 0.87 h active, 259k out — the first frontend step whose figure could be measured, since its close-out supplied the boundary the earlier ones lack). ✅ **The owner's manual acceptance pass came back PASSED on 2026-08-01** — all seven checks (the grid trap on Owner/Admin, a deactivation refusal naming its holders, disabled grant levels giving the real reason, **signing in with the exact password the dialog displayed**, the absence of a confirm-field and of Escape/outside-click dismissal, "Users" and "Roles" as two nav items, and a role description that is genuinely uneditable). **Nothing about F3 is outstanding.**
- **✅ S1 (substring search) is DONE, CLOSED OUT and LIVE-VERIFIED BY THE OWNER — standalone, backend + frontend, migrations V28 and V29.** Nothing about it is outstanding: on 2026-08-01 the owner signed in to the running stack and confirmed `/api/products?search=kit` and `/api/customers?search=πελατησ` both return correct results. That second query is the exact one that returned **zero rows** before the locale defect was found, with a green suite saying nothing was wrong, so it is the independent confirmation that matters. **Sorting (S2) is now done too** — see the S2 bullet above. `pg_trgm` +
`unaccent`, one `IMMUTABLE` normalisation function (`novocore_searchable`), **17 GIN trigram
indexes**, one shared `TextSearch` specification used by all five services, `?search=` on the five
list routes, and one `SearchFilter` component on all five screens. It closes the open decision the
frontend roadmap had refused to guess at — the Products filter box sent `sku=`, an **exact** lookup,
so typing `TEST` against eight `TEST-PRODUCT-*` SKUs matched nothing. `sku=` and `ean=` stay exact
and are what a scanner uses; customers' `by-vat-number` likewise, because it is the authoritative
auto-link and must not match approximately. 🎯 **`PROGRESS.md` carries the authoritative
16-row search target list** — every screen that will ever have a search box and the fields it
searches, including entities that do not exist yet. A step adding search adopts its row from that
table rather than re-deriving a narrower one. **Reconciling S1 against it found two gaps the green
build could not**, both closed in **V29**: `Product.brand` had never been built at all, and
`supplier.vat_number` existed but was not searched while the customer one was. **Still not built:**
`Supplier.code`, `Supplier.alias`, `Customer.code` — brief-*(draft)* fields, a
schema-plus-routes-plus-forms item rather than a search one, and that single item blocks part of six
rows. **`Product.category` is its own proposal**, deliberately not started: the requirement is three
levels deep with a product in several categories at once, so it is a self-referencing table plus a
join table, not two flat columns.
- **🐛 S1's finding is the one to carry: the test database was not configured like the real one.**
`docker/compose.yml` initialises PostgreSQL with `--locale=C` (deliberate — deterministic Greek sort
order across machines); **Testcontainers took the image default `en_US.utf8`.** Under locale `C`,
`lower()` folds ASCII **and nothing else**, so the normalisation function shipped with a bare
`lower()`, `Πελάτης Λιανικής` normalised to `Πελατησ Λιανικησ` — accents stripped, sigma folded,
**still capitalised** — and a search for `πελατησ` returned zero rows on the real server. **Nothing
errored anywhere and every integration test passed**, because the tests were describing a database
nobody runs. Caught by running the migration against the live stack, which is the only thing that
could have caught it. Fixed twice over: the function now names `lower(… COLLATE pg_c_utf8)`
(PostgreSQL 17's builtin provider — full Unicode case mapping, platform-independent, and chosen over
ICU because ICU's behaviour tracks the bundled library version and an index expression must not),
and — **the fix that matters** — `PostgresTestContainerConfiguration` now passes the same
`POSTGRES_INITDB_ARGS` as `compose.yml`, with `TextSearchIT` asserting `datcollate = 'C'` so the pin
cannot be quietly removed. ⚠️ **`REINDEX` the fifteen indexes after a PostgreSQL major upgrade** —
Unicode moves, the same caveat `unaccent`'s rules carry.
- **🔒 S1's other finding: a hidden column must leave the query, not just the response.**
`PRODUCT_SUPPLIER_SKU` is both a restricted field and a column worth searching. Redaction blanks it
in the answer and does nothing about the row still being **findable** by matching it — which
discloses the value one character at a time, every step confirmed by a result the role is entitled
to see. `ProductService.searchFor` therefore searches fewer columns for a restricted viewer. The
consequence: **the same term can return fewer rows for a restricted role**, which is correct.
- **✅ The spec now declares every primitive component required — backend item 2's primitive half, done 2026-08-01, before F4.** `OpenApiSchema.recordSchema` marks a record's primitive components required, which is **one rule accurate in both directions**: a primitive cannot be null, so on a request it is mandatory (`FAIL_ON_NULL_FOR_PRIMITIVES` refuses an absent one before any handler runs) and on a response it is always present. **2 → 78 schemas declare `required`; the spec diff is +76 lines and −0, with 174 operations unchanged; no production code was touched** — the generator lives in `src/test`. ⚠️ **That made `tsc` refuse a create form omitting a mandatory field — and Q1's item 7 partially undid it on 2026-08-03.** Boxing the eight boolean flags so the refusal names them removed their `required` declaration, because a boxed `Boolean` is not a primitive: **78 → 75 schemas.** The 20 `long` ids are unaffected and still declared. `product-create.tsx` therefore still sends `serialTracked` explicitly and is load-bearing again until **8a** restores the declaration. Three things were checked before approving it: there is **one spec artefact and no live `/v3/api-docs`** (springdoc is not a dependency — zero springdoc/swagger entries in the deployed jar — and the 401 it returns is what Spring Security answers for *any* unmapped path); the redaction claim was **generalised from `ProductView` to all 53 response records carrying a primitive, with no exception** (nothing declares `@JsonInclude`, and the two withholding mechanisms null reference-typed fields or substitute a masked string); and the anti-pattern numbering was reconciled. ⚠️ **What it deliberately did not close:** 28 schemas require a *reference-typed* field inside a compact constructor, invisible to reflection — now roadmap steps **8a / 8b**, scheduled after Q1 and before R1. ✅ **Item 7 is done** (2026-08-03), boxing the 7 boolean primitives plus a latent eighth so the refusal names the field. **All three were kept separate on purpose — and items 7 and 8 turned out to be coupled after all**, which the queue's own reasoning did not see.
- **🐛 The sweep's most useful finding was in our own tests.** It surfaced 19 type errors and **not one was in production code**: every screen test built a `Me` with no `active`, a `Role` with no `id`, a `UnitOfMeasureView`/`RoleView` with no `active` — **fixtures describing a response the server never sends.** The live probe shows `"active":true` on every `/api/me`. Nothing but the spec fix had ever noticed.
- **🐛 F3's live probe corrected a backend item rather than merely passing.** `NewUser.roleId` is a primitive **`long`**, so `POST /api/users` with it omitted answers `400 "Cannot map null into type long"` naming no field — the same defect as `serialTracked`, which `PROGRESS.md` recorded as one of *exactly two* on the surface. **That count came from a grep for primitive `boolean`**: at least **22 request records carry a primitive**, and **0 of the 50 request-body schemas on the surface declare a `required` list**. Nothing has hit them because the screens that send those bodies do not exist yet — **F5 onwards is when they will, one at a time, exactly as Products did.** Backend item 2's priority is unchanged and its scope is now much larger. The contrast that settles the design, one route apart in the same probe run: the boxed `Boolean` + `Required.field` on `PUT …/field-restrictions/{field}` answers **`"restricted" is required and was not supplied."`**
- **⚠️ `docker compose down -v` is expensive on this stack — use `docker/reset-trading-data.sql` instead.** `docker/.env` holds only the DB password, the site address and the backup encryption key; **the Google Drive client secrets and refresh tokens live in the `setting` table**, and `NOVOCORE_BOOTSTRAP_OWNER_*` are blank. So dropping the volume destroys the commissioned Drive credentials *and* the Owner account, neither reproducible from `.env`, and the consent flow has to be re-run including fresh destination folders. The reset script keeps Settings, Users, Roles, the Flyway lookups, the backup history and the audit log — **the last of these deliberately absent from its `TRUNCATE` list, because `TRUNCATE` does not fire the row trigger that makes it append-only.**
- **📋 An approved proposal is now recorded as a checklist, and close-out reconciles against it** (`CLAUDE.md`). This is a direct consequence of 15c: nothing in step 15's close-out was inaccurate, it simply summarised what was built rather than reconciling against what was agreed, and a summary cannot see an absence. Reconciliation is **step 1 of six** at close-out, and each sub-part must come out as done, explicitly deferred with a reason, or still open — **a sub-part with no verdict is itself a finding.**
- **⚠️ Proxy self-invocation is now a build failure, and finding that out cost two real defects.** A class calling its own `@Transactional` method never goes through Spring's proxy, so the annotation silently does nothing. It bit `EmailOutbox` (step 11), `RestoreVerifier` (step 12) and — found by the new rule — **`AuditLogServiceImpl`, where two unannotated overloads self-invoked a `REQUIRES_NEW` one, so audit entries joined the caller's transaction and were rolled back with the very operation they recorded.** `SelfInvocationRulesTest` enforces two narrow rules (non-transactional caller; any self-call to non-default propagation), each with a probe proving it fails. A transactional→transactional self-call with default propagation is **allowed** — forbidding it gave 44 nearly-all-harmless findings. Fixing the backup one also exposed a **latent lazy-loading crash on the untested prune path**, now covered by a test. `CLAUDE.md` names the anti-pattern for the cases ArchUnit cannot see (`@Async`, `@Cacheable`, `@PreAuthorize`, captured lambdas). **The audit-log fix is verified behaviourally, not structurally**: two new tests assert an entry survives a rolled-back transaction and is visible from a separate connection while the caller is still open, and **both were confirmed to fail against the reintroduced bug** before being accepted. That distinction is the lesson — deleting the annotation and calling the private `write(...)` directly would be structurally spotless and reintroduce the defect in full, so only the behavioural tests hold the guarantee. `AuditLogIT` previously had seven tests and not one involved a rollback, which is why it went undetected. **866 tests at that point.**
- **✅ CI is green and now genuinely covers the backups (`5a6dfa5`, run 30446419236).** The backend workflow had been **failing on `BackupIT.verifyConfigurationIsHonest`** — `pg_dump` reported `16.14` where the test requires 17. **The install was fine; the assumption about it was not.** `ubuntu-latest` already carries PostgreSQL 16 and `/usr/bin/pg_dump` is `postgresql-common`'s `pg_wrapper`, which picks a version from the local cluster rather than the newest installed — so installing `postgresql-client-17` never made the bare name mean 17. Fixed by putting `/usr/lib/postgresql/17/bin` on `$GITHUB_PATH` (naming the versioned binary instead of trusting a resolution rule) plus a **separate step asserting all three of `pg_dump`/`pg_restore`/`psql` report 17**, so a recurrence fails at the install naming the cause rather than in an integration test. **CI environment only — no application code changed**, and the runner's PostgreSQL 16 packages were deliberately left in place. **The payoff is coverage, not the tick:** `BackupIT` runs on CI with **16 tests and 0 skipped**, so the restore check really dumps, really restores into a real database and really asserts the restored ledger balances, against a real 17 client/server pair. Without the fix `BackupIT` would eventually have *skipped* — a green build silently no longer covering brief §13's outstanding risk, which the workflow's own comment names as the worst available outcome. **866 tests at that point, `BUILD SUCCESS`, counted from CI rather than a local run.**
- **✅ Step 12 is commissioned — no longer "code-complete but unverified" (2026-07-29).** All three owner action items are closed: the encryption key is generated, in `docker/.env` **and in a password manager**; both Google accounts have consented and both destinations have **`UPLOADED` a real artefact**; and the runtime image was rebuilt, installing `postgresql-client-17 (17.10-1.pgdg26.04+1)`, applying V21–V23 to the live database and taking a real dump through it. Evidence from the database, not the logs: `backup_run` id 8, 309,820 bytes, `SUCCEEDED`, both `backup_upload` rows `UPLOADED`, and retention pruning run 1 under its own stated policy. **The consent screen is published In production on purpose** — left in *Testing*, Google expires refresh tokens after 7 days, which would give a backup regime that works for a week and then fails quietly; `drive.file` is non-sensitive so publishing needed no verification review. **The key was never generated until this session** — the previous claim that `docker/.env` held the only copy was wrong; there was no copy, the running stack was still at schema V20 on a pre-step-12 image, and **no backup had ever been attempted**.
- **⚠️ Two lessons from commissioning, both worth more than the incidents.** (1) **Four secret values were stored wrapped in literal angle brackets** — `<GOCSPX-…>` — because the setup instructions used two placeholder styles, `<client secret>` in `.env` and `PASTE_CLIENT_ID` in SQL; the bracketed ones were pasted over and kept their brackets. Both uploads then failed `HTTP 401: The provided client secret is invalid` on a run whose dump and encryption had succeeded. It was diagnosed **structurally** — `ascii(left(value,1))=60`, `ascii(right(value,1))=62` — which named the defect in one query and proved it hit exactly those four values, so **no Google console trip and no re-consent were needed**. (2) The real failure was the **verification step**: it printed every value's length, including the wrong one, but **never stated the expected length**, so a 37-character `GOCSPX-` secret passed unremarked. **A check that displays a shape without asserting it is not a check.** Every configuration check now carries its expectation — including *client ids identical / folder ids different / refresh tokens different*, because two consents against the **same** account produce two working uploads into one Drive, which looks exactly like success.
- **✅ Closed incident: a refresh token was pasted into a chat session** during commissioning (the secondary account's). Revoked, re-consented, **and a fresh destination folder created under the new grant** — that last step is not optional, since `drive.file` grants access per file under a specific authorisation, so reusing the old folder id would have failed at 02:00 rather than at setup. No residual. The rule it leaves: an authorisation code is short-lived and single-use and barely matters; **a refresh token is durable and is compromised the moment it is copied somewhere it was not meant to go.**
- **All work is pushed to `origin/main`.** Convention is **one commit per build step**, and **session close-out now always commits *and* pushes** (`CLAUDE.md`) — three consecutive sessions had previously ended with unpushed commits, and the repo has no other cross-machine sync mechanism.
- **🚫 Pre-launch blocker PLB-1: no 2FA.** Decided deliberately (the app is not internet-facing yet) with an explicit condition — it **must be resolved before any external or remote access**, including Remote/Order Staff logging in from outside the local network, which is that role's whole purpose. See `PROGRESS.md`.
- **⚠️ `docker/.env` is gitignored and machine-local**, and holds exactly three variables: `NOVOCORE_DB_PASSWORD`, `NOVOCORE_SITE_ADDRESS` and `NOVOCORE_BACKUP_ENCRYPTION_KEY`. Every one-time bootstrap variable has been **removed once consumed** — the SMTP password now lives only in the `smtp.password` setting, the owner's only in the user table, and the **four Drive OAuth secrets only in Settings**. **The encryption key is the exception that never goes away**: it is read on every backup and restore and cannot move into Settings, since the `setting` table is inside the dump. ⚠️ Its `.env` copy is **43 characters where it was generated as 44** — the trailing `=` base64 padding was lost in transit. Checked rather than assumed: both forms decode to **byte-identical** 32-byte key material and Java's decoder accepts unpadded input, so either copy restores. Recorded so nobody comparing the password-manager entry against `.env` concludes the key is corrupted. A fresh clone must copy `.env.example` and set `NOVOCORE_DB_PASSWORD` or nothing starts — there is deliberately no fallback. A fresh machine also needs JDK 25 and a Docker daemon; Maven is not required, as `backend/mvnw` is committed.
- **⚠️ `docker/.env` has a UTF-8 BOM and CRLF endings.** Compose copes; a plain `grep '^KEY='` does not — the BOM hides the first key and returns an empty value that looks like a wrong password. Strip both when reading it from a script.
- **⚠️ A test cannot be pointed at a non-Testcontainers database just by setting `spring.datasource.*`.** `PostgresTestContainerConfiguration` sits inside the package `CoreTestApplication` scans, and that explicit `@ComponentScan` does not carry Boot's `TypeExcludeFilter` — so the container bean is registered by *scanning* (making `AbstractCoreIntegrationTest`'s `@Import` of it redundant) and its `@ServiceConnection` overrides whatever URL the test set. The test then **reports the URL it asked for while talking to a container**. `SELECT current_database()` settles it in one line: a Testcontainers PostgreSQL answers `test`. To genuinely reach another database, declare a boot configuration excluding `..core.testsupport..`, in the `gr.novotrade.novocoretest` sibling package. **Or sidestep it entirely, which is what `LiveSeedTest` does**: no Spring context at all, reaching the live application over HTTP through `LiveHttpTransport`. If what you want is to talk to the running system rather than to a database, that is the cheaper answer.
- **The `kostas` owner password was rotated** on 2026-07-28 through `UserService.changePassword` against the live database (policy, encoder and audit entry all applied) and verified by logging in over HTTPS. **There is still no change-password screen**, so rotating it again means the same one-off route.
- **`CLAUDE.md`** verified against intent, and now carries a **Session close-out** section: on "close the session", commit, update `docs/PROGRESS.md`, update this primer.
- **Toolchain** (per-user, no admin needed): Temurin JDK 25.0.3+9 and Maven 3.9.16 under `C:\Users\kosta\tools\`; Docker Desktop 29.6.2. A committed Maven Wrapper means `./mvnw` works with only a JDK.
- Minor open items: **4 npm advisories as of 2026-08-02** — 2 high via `react-router`/`react-router-dom`, 2 moderate via `@hono/node-server`/`@modelcontextprotocol/sdk` — deliberately unfixed for now. ⚠️ **This was recorded as "5 transitive vulnerabilities in the routing library" and was wrong on both counts**; it is a point-in-time figure, so **re-run `npm audit` rather than trusting this line**. Also: old Google Drive copy at `G:\My Drive\Novocore` pending safe removal (rename first, delete later).

## Resolved technical decisions (ADRs in `docs/decisions/`)

- **0016** **ΜΑΡΚ, UID, QR URL and transmission status are core fields; a vendor's document id is not.**
  Amends the "external reference IDs never live on core entities" rule by stating the test it always
  encoded: **would this field survive the external system being replaced?** Go's internal document id
  would not — it names a row in Go's database and becomes meaningless at step 40, which is exactly why
  it belongs in a mapping table retired with its adapter. The ΜΑΡΚ would — it is a statutory identifier
  of **Novocore's own** document, cited in a VAT return, and outlives Go, the Πάροχος and arguably
  Novocore. Same reasoning that already put myDATA codes on `VatExemptionReason` and `UnitOfMeasure`,
  so this restates a line rather than carving an exception. Consequence: transmission status is a core
  concern, so *"recorded but not transmitted"* is queryable without asking an adapter — which is what
  step 25's series gap detection needs.

- **0001** Java 25 LTS + Maven. Java 25 forces the **Spring Boot 4.x** line (3.5 does not support it), which brings JUnit 6, Testcontainers 2.x and Flyway 12. Accepted knowingly.
- **0002** ArchUnit used as a plain library, not via `archunit-junit5` — no `archunit-junit6` exists and the JUnit 5 variant binds to JUnit Platform 1.x.
- **0003** Multi-module layout so the adapter boundary is a **compile error**, not just a test failure: adapters will depend on `core-api` only, so core entities and repositories are absent from their classpath.
- **0004** **Goods Receipt creates Inventory Lots**, not Purchase Invoice posting, with a **GR/IR clearing account** absorbing the timing gap in either direction. Chosen because brief §6's myDATA-first import means goods routinely arrive before their invoice exists. **Built in step 8**; its one open item is closed by ADR 0008.
- **0005** **EUR-only behaviour, but currency is modelled from day one.** `Money` always carries a currency and every monetary column has one. No rates, no FX gain/loss.
- **Entity ids are `bigint`** (not UUID) — single self-hosted instance, smaller indexes.
- **Money is exactly 2 decimals**, normalised on construction; **unit costs and quantities are 6**. A more precise money value is rejected, never silently rounded. **`UnitCost` is that own type, built in step 6** — `extend()` is the only route back to `Money` and it names its rounding mode, so precision is only ever given up where the code says so. Zero is a valid unit cost (a free sample is a real lot); negative is not.
- **Every monetary column carries a `char(3)` currency companion**, named `<column>_currency`, tied by a biconditional CHECK. Settled by the schema's first monetary column, `product.selling_price` in step 5, and enforced across the whole schema by `SchemaConventionsIT`. A JPA field mapping a `char(3)` needs `@JdbcTypeCode(SqlTypes.CHAR)`. **Since step 6 the rule also covers monetary `numeric(19,6)` columns, discriminated by a `_cost` suffix** — scale alone cannot tell a unit cost from a VAT rate or a quantity, so the name has to.
- **`Quantity.times(Quantity)` is exact and throws rather than rounding.** Two 6-decimal quantities multiply to 12 places, so the naive version fails on its own trailing zeros; a product that genuinely needs more than 6 places is a modelling error, not a rounding question.
- **Attachment content is stored in PostgreSQL** (`bytea`), so backup and restore stay one atomic artefact. **Stored once**: since V21 an emailed document is referenced by the outbox, never copied into it (ADR 0012).
- **0006** **Correction policy (Q13): documents are immutable, our own records are editable.** Invoices and credit notes are corrected by a reversing entry; receipts, payments, transfers and manual entries are edited in place with the previous state written to the audit log. The line drawn is whether the record exists *outside* NovoCore. Enforced in the database, and the policy is stated once as an SQL function both triggers call, with a test proving it agrees with the Java enum. **The inventory write-off was added to the immutable list** — flagged as an addition and explicitly approved.
- **0007** **VAT posting (Q14): separate Output VAT and Input VAT, never netted.** Per-line computation summed by rate, which is why a journal line carries its VAT class and taxable base — without them two Output VAT lines at different rates are indistinguishable amounts. Reverse charge needs no new structure; exempt lines post no VAT line at all.
- **0009** **Open item matching is a layer over AR/AP, not a second ledger: documents post, allocations post nothing.** Both ends of an allocation are already in the same control account, so an entry for it would debit and credit that account for the same amount. Consequences: **no document stores an open amount and nothing stores a "paid" flag**, and an allocation can be reduced or released without touching a posted entry — which is what makes **Q13's second half** implementable at all. `release` deletes a row, the one place this schema does, because an allocation is a statement about a current relationship rather than a record of an event. The ADR also carries **Q15's remainder** (confirm at entry, record the confirmation, no queue) and **Q10** (the retail customer is seeded and structural).
- **0010** **Q18: a lot's share of an allocated freight cost splits by what is still in the lot.** The part belonging to stock on hand raises that lot's unit cost (brief §5's "includes allocated landed costs"); the part belonging to stock already gone posts to a new **`Landed cost variance`** account, the exact counterpart of ADR 0008's purchase price variance and in the same group — because those units are not in the lot to carry it and the COGS that took them out is immutable. **A lot's cost is now two figures**: `received_unit_cost`, frozen for the life of the lot, plus `allocated_landed_unit_cost`, with the carrying cost their sum computed on read. The freezing is load-bearing — allocation is computed against the received cost, so two freight invoices covering one shipment split the same way whichever order they are entered in. An allocation names the **purchase invoice expense line** it spends, so the unallocated remainder stays answerable; lots are always named, never inferred.
- **0011** **Stock coming back into a re-costed lot: returns catch up, reversals refuse.** A defect ADR 0010 introduced, found by *reviewing* step 10 and measured (€20 on a ten-unit lot) rather than reasoned about. A **return** says the sale was real, so the allocation's split was right and only the returning units' share is owed — it posts a catch-up (debit Inventory, credit `Landed cost variance`) in the same entry, leaving COGS credited exactly what was debited so ADR 0009 stands. A **reversal** says the movement never happened, which would mean the allocation computed the wrong *split* — so `reverseConsumption` and `reverseWriteOff` refuse on a re-costed lot and name a remedy that a test actually walks. **The asymmetry is deliberate**; a later reader should not try to make the two consistent. V19 stores `stock_write_off.unit_cost` because of it — the entry gives the amount but not the six-decimal cost behind it.
- **0013** **Backups: encrypted before they leave, off-site reported separately from success, proven restorable.** The key is an environment variable *because* Settings is inside the dump — the one fact that decides it, and the reason this is not a reversal of step 11's SMTP decision. `NOT_CONFIGURED` is its own upload status, so an off-site copy nobody set up is visible rather than absent. Retention is pure logic in its own class because its mistakes are the only ones in this codebase that cannot be undone. The restore check asserts **the restored ledger balances**, which is what turns "the file restored" into "the books restored". Plain `HttpClient` over the Google SDK, for the same testability reason as ADR 0002.
- **0015** **A lot's movements post the change in its carrying value** (answers Q45; revises the *arithmetic* of ADRs 0008/0010/0011, none of their decisions). A lot is carried at its remaining quantity extended at its unit cost, rounded once — one definition, `LotValuation` — and every posting that moves it puts the **change in that figure** on the Inventory line. Not `quantity × cost` rounded, which is a different number and drifted a cent per movement on any sub-cent-cost lot. **The rounding mode is fixed at `HALF_UP` and deliberately does not follow `ledger.rounding.mode`**: a lot's value at two moments must be measured the same way, and a setting somebody can change cannot promise that. `InventoryServiceImpl` therefore reads no settings at all now. **The freight allocation changed too** — its capitalised half is the lot's real increase rather than a proportional estimate — which makes the variance half a remainder that can be one cent negative, hence **migration V24** dropping that CHECK. **Reversal is the one place the rules pull apart**: a mirror must be exact, so it is only right if the lot has not moved; `reverseConsumption`/`reverseWriteOff` compare the two and refuse **iff** they differ, which cannot fire on a whole-cent cost and never fires when reversing the most recent movement. Two consequences to expect: two identical units of one lot can cost 12.50 then 12.51, and cost is now path-independent.
- **0014** **Property-based tests against our own harness, not jqwik.** The third instance of ADR 0002's situation and resolved the same way — *take the idea, not the artifact* — except that jqwik has no plain-library form, so ~500 lines were written instead: `Gen` (sample plus **shrink**, deliberately with no `map`, because a mapped value cannot shrink), `Property` (500 cases, or 20 for database-backed ones) and edge-biased `ValueGenerators`. Published from `core-api` as a test-jar so `core` shares it. **The fixed default seed is the load-bearing trade-off**, and it is a trust decision rather than a technical one. `PropertyTest` proves the runner actually fails, for the reason `allowEmptyShould(true)` taught.
- **0012** **An emailed document is referenced, not copied; a missing file degrades, never breaks.** Revises the half of step 11's `EmailAttachment` reasoning that was wrong, and keeps the half that was right (an inline attachment is stored rather than regenerated at send time, because a PO PDF's underlying data can change between approval and dispatch). Two shapes, one per row, enforced in Java and in the schema. `ON DELETE SET NULL` is the whole deletion policy: history is neither erased by a document's deletion nor allowed to prevent it. **Splits Q43 in two** — cheap rows, expensive inline bytes — and makes the second prunable without a schema change.
- **0008** **Three answers sharing one principle: a posting that reflects a physically verified event does not change once other things depend on it.** (1) *ADR 0004's open item* — a price difference posts to a **purchase price variance** account and the lot keeps the cost it was received at; re-costing a lot FIFO has already consumed into posted COGS is the same problem as editing a posted entry, expressed as a number. (2) *Q17* — a sale may drive **aggregate stock negative**; it is recorded as a shortfall, subtracted from the sellable figure so the product genuinely reads negative, queryable for review, and never retro-costed. (3) *Q39* — a **Goods Receipt is immutable**, corrected by a reversal that un-receives the lots and posts the mirror together, and refuses once they have been touched. **Constrained Q18**, which ADR 0010 then answered within that shape: allocate against the received cost, never reach back into consumption already costed out.

## Architecture (non-negotiable)

- **The core owns its data model** — never shaped by an external system's schema (Go, WooCommerce, etc.).
- **Ports and adapters**: adapters translate external systems into the core's model; a black-box swap only touches its adapter. Internally-driven functionality is a **module** instead.
- **Field boundary rule**: a field belongs in the core only if the core's ledger/reports/modules genuinely need it. External system reference IDs (Go ID, Woo ID) never live on core entities — each adapter keeps its own mapping table.
- **Governance**: adapters/modules call the core only through defined interfaces (enforced by ArchUnit); contract tests per adapter; fail-loud, never silently guess.
- **Performance**: core operations complete instantly, never synchronously wait on an external adapter call.
- **Never silently resolve ambiguity** — this shows up everywhere: rounding, customer matching, bank matching, invoice categorization. Auto-resolve only what's certain; suggest and require confirmation otherwise.
- **Code-quality governance beyond structural boundaries**: automated tests for core logic, prefer root-cause fixes over patches, periodic cleanup passes — architecture alone doesn't prevent patch-accumulation spaghetti.

## Tech stack

**Backend:** Java + Spring Boot, PostgreSQL, Docker, self-hosted (HTTPS reverse proxy from day one). Ruled out: SQLite, Python/PHP backend, cloud hosting for v1, bank payment initiation, cloning a vendor's feature set.

**Frontend:** Vite + React + TypeScript + Tailwind + shadcn/ui, in `/frontend/`, separate from backend. Default shadcn theme until Claude Design sets the real brand look. Ask (don't guess) when a UI element could reasonably use more than one shadcn component.

**Shared core services:** Email sending and document attachments — one mechanism each, never one per module.

**Email — built (step 11).** SMTP configuration lives in Settings, reached only through `EmailSender`. **An ArchUnit rule confines `jakarta.mail` and `org.springframework.mail` to `..core.email..`**, so a module growing its own SMTP client is a build failure rather than a discovery; proven to fail against a probe. **`EmailMessage` has no From and no Reply-To field** — both are configuration applied to every message, because a caller able to override them could route replies to a mailbox nobody reads, which is the exact failure `smtp.reply-to` exists to prevent. **Asynchronous by an outbox table** (rule 4): `send` writes a row in the caller's transaction and returns, so a message is queued **if and only if** the operation that queued it committed — a rolled-back Purchase Order sends no PDF with no compensating logic. A scheduled dispatcher does the SMTP conversation with no transaction open across the network; the accepted cost is a possible duplicate if the process dies mid-send, which is the right direction to fail. **Retry is exponential to a ceiling and a message that gives up stays `FAILED` and queryable, never dropped**; re-queueing is manual, because whatever was wrong is still wrong. `smtp.start-tls` (declared in step 2, never used) became **`smtp.transport-security`** — a boolean cannot distinguish implicit TLS on 465 from STARTTLS on 587, and a STARTTLS client aimed at 465 **hangs rather than failing**. Scheduling is enabled in `app`, not the core, so the core's tests drive the dispatcher by calling it and nothing sleeps.

**Outbox retention — Q43 answered and built (V22).** Rows `FOREVER`, inline copies of generated attachments **90 days**, both in Settings; `EmailRetention` runs daily. Referenced documents are never pruned by it. See the Q43 entry below for the guards, which are the substance.

**Attachments on email — referenced, not copied (V21, ADR 0012).** `EmailAttachment` has **two shapes and a row is exactly one of them**, enforced in the constructor and by CHECK constraints: `stored(attachmentId)` references an `AttachmentService` document and carries no bytes, while `.pdf(...)` / `.of(...)` carry bytes for a file that exists nowhere else. So emailing a document that is also attached to a core record stores it **once**. Reference-only was considered and rejected — it would move report bytes rather than save them, and fill a "documents on core records" table with things that are neither. **The recipient is unaffected either way**: SMTP transmits real bytes, resolved at send time, and `compose` never sees the distinction. **Reading is transparent**: `attachmentsOf(emailId)` then `downloadAttachment(id)` behave identically for both shapes, so viewing what was sent is one action from the sent-email record with no separate lookup. **Deletion degrades rather than breaking** — `ON DELETE SET NULL`, chosen over CASCADE (which would erase the record that the message had an attachment) and RESTRICT (which would let a 2026 email pin a document forever); the entry keeps the filename, size and checksum and reports it unavailable *with the reason*, and availability needs no query because a non-null `attachment_id` is itself the proof. `EmailAttachmentUnavailableException` is deliberately distinct from the `IllegalArgumentException` for an id that never existed. A document deleted **before** dispatch fails that message alone and loudly, through the same guard that isolates a poison row — a mail is never sent with an attachment silently missing. **`content_source` is stored, not inferred**, because once the bytes are gone both shapes are an empty row and the history must still say which happened.

## Domain model summary

**Chart of accounts — built and committed (step 3).** The full account list, decisions and
per-step obligations are in `docs/PROGRESS.md`. Resolved shape:

- **Two levels only:** an `AccountGroup` entity (name + `displayOrder`) with accounts under it. Not a self-referencing Account tree. Groups are an entity rather than flat text because ordering is **manual/drag-and-drop**, which needs somewhere to store a group's position. Alphabetical ordering applies only to sub-ledgers (customers, suppliers); inventory sorts by SKU.
- **Normal balance side is derived from account type, never stored** — there is no `normal_balance_side` column and a test asserts its absence. **Seven types**, because both contra types are genuinely needed: `CONTRA_ASSET` (accumulated depreciation is Asset-classified with a *credit* normal balance; without it fixed assets report at roughly double carrying value) and `CONTRA_INCOME` (sales returns are Income-classified with a *debit* normal balance; typed as `EXPENSE` they would sit below the revenue line and overstate gross revenue).
- **Kinds:** Standard / Bank-Cash / Partner Clearing / Control (with a declared sub-ledger type). `type` and `kind` are independent dimensions — accumulated depreciation is `CONTRA_ASSET` *and* `CONTROL`. Control accounts: Accounts Receivable→Customer, Accounts Payable→Supplier, Inventory→Product-Lot, Fixed Assets→Asset, and **GR/IR clearing→Supplier**. A biconditional CHECK enforces "Control iff sub-ledger" in the database, not just in Java.
- **Bank-Cash:** Cash, Alpha Bank, Piraeus Bank, NBG. **Partner Clearing:** Skroutz, ACS Courier, POS provider, **plus PayPal and Stripe** — the latter two grouped under Cash & Cash Equivalents but treated as clearing accounts, so processor fees post as expense on receipt.
- **Account codes are left blank** and ΕΛΠ mapping is null for now (comes from the accountant later). Because neither is usable as a handle, **`AccountSystemKey`** gives the eleven accounts NovoCore's own posting rules must locate a stable machine identifier. Keyed accounts can be renamed and reordered but never deactivated, and the key is never settable from application code.
- **No account balance is stored anywhere** — a balance is the sum of its journal lines, computed on read from step 7. So step 3 introduced no monetary columns at all.
- **`expectedToClear` flag** rather than a fifth kind, for accounts whose residual balance is a real discrepancy: Freight/Landed Cost — Unallocated, GR/IR clearing, Unclassified — Needs Review.
- **Sales is split by channel:** **Store & Phone**, eCommerce, Skroutz — phone named explicitly rather than left to convention. **Sales returns are contra-revenue, one account per channel**, so credit notes keep return rate visible per channel instead of netting into revenue.
- **New accounts** not mapping from Manager: the three expected-to-clear ones, the three Sales returns, `Rounding differences`, `Inventory write-off / shrinkage`, and `Interest expense` in its own **Finance Costs** group (below EBIT, so EBITDA/EBIT stay meaningful).
- **Inventory write-off sits in the COGS group**, its own account separate from Cost of goods sold — gross margin reflects the loss, sale-driven COGS stays clean. **One account with a reason code**, not three; the reason is `WriteOffReason` (built in step 6), and **carrying it on the write-off transaction is now a step 7 obligation**. Distinct from the Damaged Goods *Location*, which marks stock unsellable but still an asset: **moving a lot there posts nothing**, so **phase 8 Clearing Checks must surface lots aging in it** — the agreed compensating control, whose query step 6 built.
- **No delete, only deactivate**; with no period locking no account is ever safely finished with. A reorder must name every member exactly once rather than leaving the rest in an order nobody chose.
- **Dropped:** Suspense, Inter Account Transfers (Manager had the latter under Equity — the error the brief corrects), and DDP (superseded by Freight/Landed Cost — Unallocated).
- **Known imperfections accepted:** `Interest received` stays in `Income` above EBITDA, so EBITDA is approximate; no current-portion split on the NBG loan; `VAT payable` was a single account, replaced in V14 by the Output/Input split (Q14); `Amortization` is seeded although nothing can post to it.

**VAT — built (step 3b), real data from Prosvasis Go and AADE.**

- **`VatClass` is a runtime-editable entity, not an enum**, because Greek rates change by statute (the 3% and island-reduced 4% classes exist only because of αρ.31 ν.5057/2023). Fields: code, description, ratePercent, active, nullable self-referencing reducedCounterpart.
- **Nine classes seeded, eight distinct percentages** — `0`/`1030`/`1040`/`1041`/`1060`/`1091`/`1131`/`1170`/`1410`. 4% appears twice: `1040` in its own right, `1041` as the island-reduced counterpart of 6%. **So the code is the identity, never the rate** — there is deliberately no `findByRate`, and a test asserts its absence.
- **Island-reduced mappings seeded as data** (24→17, 13→9, 6→4 as `1041`, 4→3), held on the mainland rate pointing at the reduced one. Enforced one level deep, lower-rated, one-to-one, never self-referencing. **No automatic rate switching by shipping destination** — future scope by explicit decision.
- **Rates are percentages** (`24.000000`, not `0.24`) in `numeric(19,6)`, with a CHECK allowing **exactly 0 or 1–100**. The lower bound is the load-bearing half: a plain 0–100 range does not catch `0.24` written for 24%, which was a real defect until V10. Zero stays valid because the zero-rated class is distinct from an exempt line. **Rates are never editable in place** — a rate change is a new class plus deactivation, because editing would retroactively change what already-issued invoices appear to have charged.
- **Precedence rule, stated as code** in `VatClassPrecedence`: **invoice line beats customer beats product**, returning which level won. **No fallback rate** — it throws rather than assuming 24%, since a silent default produces a plausible invoice at a rate nobody chose and an undercharge is unrecoverable after issue.
- **`VatExemptionReason` is a separate entity, not a 0% rate** — zero-rated charges 0% under a real rate; exempt is outside VAT because a named article of the Κώδικας ΦΠΑ says so, and myDATA reports them differently. **Seeded (V8) with the real 29 rows from Prosvasis Go**, in the recodified article numbering (άρθρο 2 και 3, 5, 17, … 58). Gaps at codes 24 and 28 — absent from *Go's* list, so worth confirming with the accountant before phase 7. `mydataCode` is stored verbatim, and that turned out to be load-bearing: codes 12 and 13 name "Πλοία Ανοικτής Θαλάσσης" in their description and not in their myDATA string, so composing the value would have transmitted those two wrong. **`mydataCode` is nullable**, because the OSS/IOSS reasons (29–31) have no myDATA mapping in Go — NULL means "no mapping exists", and phase 7 must refuse to transmit one rather than compose a substitute.
- **`ChargeType`** — extensible lookup for fees charged *to* the customer as revenue: name, default VAT class, income account, active. The service **refuses a non-`INCOME` account**, which is the point of it existing: wiring a delivery fee to `Transportation costs` to net it off would understate revenue and cost together. **Seeded (V7) with `Delivery` and `COD fee` against dedicated `Delivery income` and `COD fee income` accounts** (Q27) — a residual bucket holding most invoices' fee lines would be the largest income line and useless as a residual. Both default to 24%, and **Q33 is settled and confirmed with the accountant: a fee's VAT rate is independent of the products on the invoice** — a 13% order still carries 24% delivery. It was raised here as a possible defect on the general principle that an ancillary charge follows the main supply's rate, and the accountant confirmed the treatment as built, so this is settled rather than a recommendation that was overruled. Nothing should later be built to derive a fee's rate from the lines around it. Nothing consumes charge types until step 9.
- **`numeric(19,6)` now covers three things** — quantity, unit cost, and *rate*. The distinction that matters is amount (2dp, because that is a cent) vs multiplier (6dp, must not lose precision before the product is rounded once).
- ~~**Still open: Q14**~~ — **closed in step 7** (ADR 0007): separate Output/Input accounts, per-line computation summed by rate, the class and base carried on the posted line. Q28 (dispatch purpose) is answered as a recommendation only, nothing built.

**Core entities — Product, Customer, Supplier and Asset built (step 5).**

- **Product:** own SKU (unique) and EAN (unique when present), `GOODS`/`SERVICE`, a unit of measure **reference** (a runtime-editable lookup since V11, carrying `allowsFractionalQuantity` and a nullable AADE myDATA code), a **required** default VAT class (there is no fallback rate anywhere, so a product without one could not be invoiced), an optional selling price, one optional supplier plus that supplier's own code. **No Go/Woo ids, no stock column, no last-purchase-price column** — both derived from lots (step 6). **A zero price is refused**; null means "not priced yet", because zero produces an invoice line worth nothing without anyone choosing to give the goods away.
- **Q5 answered: one product, one supplier**, a plain nullable foreign key, no many-to-many. **A supplier SKU without a supplier is refused** in code and by a CHECK — that meaningless state was the whole question.
- **Customer / Supplier:** name, single email, single phone (Q8), VAT number (unique when present), `VatStatus`, and for Customer a nullable VAT class override feeding the precedence rule. **Customer names are not unique; VAT numbers are** — two retail customers can genuinely share a name.
- **Q9 answered: `VatStatus` is shared by both parties** — `DOMESTIC`, `INTRA_EU_B2B`, `NON_EU_EXPORT`, `EXEMPT`, `OTHER`. **Five, not four:** export and intra-EU B2B are both VAT-free under *different* articles and are reported differently, so folding export into "other" would lose what has to be stated. Intra-EU requires a VAT number, exempt requires a `VatExemptionReason`; both definitional and both database constraints. **No VIES validation** — that adapter is phase 7.
- **Matching is split by certainty** (rule 7): `findByVatNumber` is exact and may be applied automatically; `suggestMatches` on name/email/phone returns candidates a human confirms. A blank VAT number matches nothing. **Merge is not built** — brief §5's alias-forward needs the ledger, and half of it would be a merge that appears to work and loses references.
- **Asset — a register, not a valuation.** Name, optional code, acquisition date, an optional later depreciation-start date, `IN_USE`/`DISPOSED` with a disposal date required exactly when disposed, and **a manually set annual straight-line rate (Q12)**. **No monetary field at all**: both fixed-asset control accounts declare `ASSET` as their sub-ledger, so cost and accumulated depreciation are sums of journal lines. No useful life (it is `100/rate`), no salvage value (Greek tax depreciates to zero), no method field (straight-line only).
- **The rate is nullable, and that is the current state**: the statutory rates per category are pending the accountant. Null means "not yet known"; nothing was guessed and no category table was created. `withoutDepreciationRate()` lists the assets still waiting. The rate is bounded **1–100** — the lower bound is the load-bearing half, because a plain 0–100 range cannot catch `0.1` written for 10%.
- **Step 4's field restriction mechanism is applied by `ProductView.redactedFor(RoleView)`, the single implementation.** Hiding the supplier also hides the supplier's SKU, since that code identifies the supplier indirectly. ⚠️ **Since V26 no role restricts anything**, so this is tested against roles created at runtime rather than against the seeded one — see the permissions section. ⚠️ `ProductService` has unredacted reads for the core's own costing rules and `...For(viewer)` variants that redact — **a named convention, not an enforced one**; the first Products controller must be reviewed for it.
- **Product gained two flags in step 6:** `serialTracked` (individually identified stock — brief §5's coffee machines) and `bundle`. Both are refused for a service; a bundle cannot be serial-tracked; neither can be changed once the product has lots. `isStocked()` is now `GOODS && !bundle`, because a bundle is typed GOODS and still holds no stock of its own.

**Inventory — built (step 6). Q7, Q25 and Q11 all answered.**

- **Q7: stock is exposed per location plus a computed sellable figure, and sellable is the Inventory location only** — excluding Damaged Goods and Service. `InventoryService.stockOf` returns `StockLevels`, which carries every location (zero where empty) and derives `sellable()` from `StockLocation.sellableLocations()`, so a second sellable location is one edit rather than a hunt for hardcoded `INVENTORY`. Nothing is stored (brief §5); it is computed on every read. This is why a single stock number was always wrong: nine on hand and three sellable is the ordinary case.
- **`StockLocation` is an enum, not a table** — the opposite call from `VatClass` and `UnitOfMeasure`, because these three values are NovoCore's own and each has behaviour attached: `INVENTORY` is what sellability is computed from and `DAMAGED_GOODS` is what phase 8's Clearing Checks must single out. A runtime-added row would be storable and unhandled. **Not a warehouse** — these are states of stock, not places on a map. Named `StockLocation` because a bare `Location` reads as an address (Q37).
- **A lot has two shapes, and the rule is: location lives wherever the quantity does.** Pooled stock stores its quantities and location on the lot. A **serial-tracked lot stores neither** — the quantity *is* the count of its units, and each unit carries its own location, because three machines received together are one lot and one going out for repair must not move the others. Storing a quantity on a serial-tracked lot would be a second copy of a derivable number, the argument that keeps a cost off `Asset`. One CHECK refuses any third shape. Consequently `moveLot` refuses a serial-tracked lot and `moveUnit` refuses one not on hand.
- **Moving to Damaged Goods still posts nothing** (the step 3 decision), and step 6 built the query the phase 8 compensating control needs: `lotsAt(location)` covers both shapes and excludes exhausted lots.
- **Serial numbers are unique across all stock.** A stronger claim than the world strictly supports, and still right: within one business, a repeated serial is overwhelmingly a duplicate scan, and catching that beats accommodating a collision nobody has seen.
- **Q25: `WriteOffReason` is a fixed enum** — `SHRINKAGE` / `DAMAGE` / `EXPIRY` / `OTHER`. Reportability is the entire reason one write-off account was chosen over three. **⚠️ It has no consumer yet**: a write-off derecognises an asset, so it needs the journal engine, and reducing a lot without the entry would leave the stock gone and the balance sheet carrying it. **Step 7 obligation** — including brief §5's exception that a *serialized* write-off uses the named unit's own actual cost, with no FIFO.
- **`SerializedUnitStatus.SOLD` is declared and unreachable**, deliberately: brief §5 wants the customer/invoice link on a sold unit, and that is step 9. The stock count already reads the status column, so it will be right the day a unit sells.
- **Last purchase price** comes from the most recent lot, by acquisition date rather than insertion order (Q6) — and **since step 10 from that lot's *received* cost**, discharging the obligation V12 recorded. One residual is stated rather than hidden: where a delivery preceded its invoice and the invoice disagreed, ADR 0008 keeps the lot at the received cost, so this is the last price we *believed*, not necessarily the last a supplier *invoiced*.
- **`Section.INVENTORY` is new and separate from `PRODUCTS`, because of cost.** Stock *levels* are a product-level read that Remote/Order Staff needs; a *lot* carries its unit cost. ⚠️ The original reason for the split was that `PRODUCT_LAST_PURCHASE_PRICE` kept cost from that role; **V26 removed that restriction**, so the section split now stands on the plainer ground that a lot is a different kind of record from a product. No grants seeded — default-deny.
- **Not built in step 6, all since delivered:** the lot's source-document reference and FIFO consumption both landed in **step 8** (ADR 0004, ADR 0008), and the **write-off table in step 7** once there was a ledger for it to post to. Still absent by decision: **no valuation column** — a lot's value is its remaining quantity extended at its unit cost.

**Bundles — built (step 6), Q11 answered "build now", to brief §5 in full.**

- A bundle is a **Product** with its own SKU and a component list, so a scan, a price tag, a Woo listing and an invoice line can all treat it as one sellable thing.
- **No stock of its own.** It cannot receive a lot, and a product that already has lots cannot become one — either way the same goods would be counted twice. `stockOf` on a bundle computes how many could be assembled per location, limited by whichever component runs out first, by **integer** division: half a component is not half a bundle.
- **`BundleAllocation` is exact integer arithmetic in cents**, largest-remainder, with ties broken by position so the answer is reproducible. Consequently it **never needs a rounding mode and never produces a rounding difference** — `Rounding differences` is for reconciling against an *external* document, not for absorbing our own arithmetic.
- **`BundleDecomposition` enforces in its constructor that the component lines sum to the bundle line.** That is what makes brief §5's "linked, not duplicated" a property of the data: either level gives the same revenue, and adding them together is visibly double-counting. The phase 8 *report* is still phase 8's.
- **Components are one level deep** — a component may not itself be a bundle. Same rule and reasoning as `VatClass`'s island-reduced counterpart: cycles become impossible by construction and allocation stays single-pass.
- **`define` replaces the whole component list, never merges**, so a bundle is never left half-changed and never exists empty (flag and components are set in one transaction).
- **A bundle or a component may be GOODS or SERVICE.** A machine sold with its installation is real; the installation takes allocated revenue and nothing off a shelf, so only *stocked* components constrain availability, and a bundle with no stocked components refuses to report stock rather than answering zero.
- **An unpriced component refuses decomposition** rather than weighing zero — that would push all the revenue onto the priced components and report the unpriced one as pure margin. Same stance as having no fallback VAT rate; `bundlesWithUnpricedComponents()` surfaces it before a sale.
- **Deactivating a component an active bundle needs is refused**, naming the bundles — the same stance as a unit of measure a product still uses.
- **⚠️ Step 9 obligation:** dissolving a bundle that has been sold would strand decomposed component lines; brief §5's "alias forward, never rewrite history" is the shape of the answer and it needs the ledger.

**Journal entries — built (step 7). Q13, Q14, Q19, Q26, Q15 and Q16 all answered.**

- **Two-layer** as the brief has it: typed transactions produce raw balanced entries. Step 7 built the lower layer plus the one typed transaction that *is* a raw entry, the Manual Journal Entry. The other seven arrive in steps 8 and 9.
- **Debits equal credits is a `DEFERRABLE INITIALLY DEFERRED` constraint trigger**, checked at commit, so `CLAUDE.md` rule 6's "structurally" holds against a `psql` session too. Deferral is load-bearing — an entry is legitimately unbalanced between its first line and its last. **Two triggers**, because a trigger on the lines never fires for an entry with *no* lines, which would otherwise be storable. Stored total columns with a single-row CHECK were rejected as a second copy of what the lines say.
- **A named side plus a strictly positive amount, never signed.** Zero is refused as firmly as negative: a zero line balances while stating nothing.
- **Sub-ledger reference required on Control-kind lines** and matched against the account's declared type; **the referenced row is checked to exist by trigger**, using dynamic SQL, because the reference is polymorphic and cannot be a foreign key — and doing it in Java would make the ledger depend on the inventory service, which already depends on the ledger. Permitted on non-Control accounts, which COGS genuinely uses.
- **No balance is stored anywhere.** `balanceOf`, `subLedgerBalanceOf`, `linesOf`, `linesFor` and `trialBalance` all sum on read. A balance on the *wrong* side reads negative rather than being made positive — an overdraft and an overpaid supplier are real and an absolute value would hide them.
- **Q13 (ADR 0006):** immutable documents, editable own-records, nothing ever deleted, reversals verified to be exact mirrors, an entry reversible at most once. A reversal is posted through the ordinary path with `reversalOfEntryId` set, which is what lets a service that owns state outside the ledger reverse its own document without a second unguarded write path.
- **Q14 (ADR 0007):** Output/Input VAT split (V14 repurposed the single `VAT payable`, safe only because nothing had posted yet), VAT class and taxable base on the line, `vatTotals()` to read them back so the columns have a consumer.
- **Q19:** all six typed transactions are Phase 1. **Q26:** a credit note is its own transaction type, immutable like the invoice it corrects. **Q15:** rounding compared **per document**, as the brief says. **Q16:** unallocated credit is a **standalone credit document**, not a bare AR balance. The last three were decisions with nothing built; **all three are built in step 9.**
- **Deliberately absent, as revisited in step 8:** `GOODS_RECEIPT` now exists as a source (Q39 answered); **`source_id` was decided against** rather than added — the document holds the link, one direction. Still absent: a human-facing entry number (Q40), period locking, and any HTTP route.

**Inventory write-off — built (step 7), discharging the step 3 and step 6 obligation.** `stock_write_off` carries `WriteOffReason`, reduces the lot or the named unit **and posts, in one transaction** — either alone is worse than neither. Brief §5's serialized exception is honoured **by construction**, since a write-off always names its lot and nothing picks one for the caller. **A lot carried at zero cost posts no entry and the stock still leaves**: a free sample derecognises nothing, and the ledger rightly refuses a zero-amount line. Reversal restores the quantity or the unit status and posts the mirror together, which is why `JournalService.reverse` refuses this source outright and names `InventoryService.reverseWriteOff` instead.

**Purchasing — built (step 8). ADR 0004's open item, Q17 and Q39 all answered as ADR 0008.**

- **Two documents, either order.** A **Purchase Invoice** is recorded, never issued: it carries the supplier's own number (unique per supplier among documents that stand), is immutable (Q13), and its inventory lines debit **GR/IR clearing** rather than Inventory. A **Goods Receipt** is the inventory event: one lot per line, debit Inventory once per lot, credit GR/IR against the supplier. `InventoryService.receive` is the lower layer it calls and posts nothing on its own.
- **Matching happens when the second document is created**, and there is deliberately no later matching operation — it would need a journal entry belonging to no document, and an unmatched GR/IR balance is exactly what ADR 0004 says it is. Both halves are queryable (`linesAwaitingDelivery` / `linesAwaitingInvoice`) for phase 8's Clearing Checks. `gr_ir_match` is a table, not a foreign key either way, because partial delivery makes it genuinely many-to-many; **it carries no money**, since the variance is stored per invoice line as the residual that makes the line's debits sum to what was charged.
- **The variance can only arise goods-first.** A receipt matched to an existing invoice line takes its cost *from* that invoice and is refused if it restates one — so invoice-first clears exactly and produces nothing to vary.
- **An accepted residual:** one receipt line matched by two invoices can leave a cent in GR/IR. Deliberate — GR/IR is `expected_to_clear` and `ledger.rounding.threshold` already exists to say what is noise.
- **Expense lines exist; automatic categorisation does not.** A line names its account. Brief §7's suggest-from-product-then-supplier-then-`Unclassified` rule is about *choosing* a destination for an invoice nobody typed, and belongs with the myDATA import. **Nothing lands in `Unclassified — Needs Review` by accident.**
- **Every line states either a VAT class or an exemption reason**, never both and never neither. **Reverse charge is a flag that must agree with the supplier's `VatStatus`** — required for `INTRA_EU_B2B`, refused otherwise, never inferred.
- **`source_id` on `journal_entry` was decided against**, discharging the step 7 obligation by answering it: an immutable entry cannot be updated after posting, and storing the link both ways is two copies of one fact. The link lives on the document, one direction, as V15 does for reversals.
- **`SerializedUnitStatus.UNRECEIVED` is new**, for reversing a delivery of machines — and it is the one status that **does not hold its serial number**, since the commonest reason to reverse a delivery is that it was entered wrong.

**FIFO consumption — built (step 8), Q17 answered.** `InventoryService.consume` reduces lots in `lotsOf`'s order (acquisition date, then id), restricted to sellable locations, and posts **one line per lot** (debit COGS, credit Inventory, both carrying the lot reference) in the same transaction. **Pooled stock only** — a serial-tracked product is refused, naming step 9, because selling a named unit means marking it `SOLD` and brief §5 wants the customer and invoice on it. **A shortage does not block**: the unfilled part is recorded, `stockOf` subtracts outstanding shortfalls so the product reads **negative**, and `consumptionsWithShortfall()` is what a review reads. **No COGS is posted for the shortfall** and a later receipt does not retro-cost it; the correction is to reverse and consume again.

**Sales, credit notes and settlements — built (step 9). Q10, Q15's remainder, Q16 and Q26 answered; Q31 confirmed.**

- **The Sales Invoice is recorded, not issued** — Go is the invoicing system of record until phase 11, so `document_number` is Go's and `stated_total` is what Go's document says. Immutable (Q13). **A sale posts two entries**: revenue here, cost of goods sold from `InventoryService`, linked by the consumption the line points at.
- **`SalesChannel` picks the revenue account** — the only place channel exists in the model, which is why step 3 split Sales and Sales returns three ways. A `SERVICE` product credits `Services` instead.
- **`SettlementMethod` picks the debit account** (brief §6): Cash, Card/POS, Skroutz, ACS COD, PayPal and Stripe debit their own account and the invoice is **born fully settled**; `BANK_DEPOSIT` and `ON_ACCOUNT` debit AR and it is an open item. **Bank deposit deliberately does not settle on entry** — a customer saying they transferred is not the money arriving; the Bank Aggregator (phase 6) confirms it. **Cash is hard-blocked at €500** (N. 5301/2026): the one refusal in this design that offers no confirmation, because the confirmation nobody can give is legality.
- **The VAT dimension is supplied** (the step 7 obligation): every Output VAT line carries its class and taxable base, per line summed by class. **`vat_class_source` is stored**, so "why is this line at 13%?" stays answerable after the customer's override has changed.
- **Bundle decomposition is materialised on the invoice**, which is what makes dissolving a sold bundle safe rather than refused — brief §5's "alias forward, never rewrite history" with no alias table, because there is no history to rewrite. ⚠️ **A dual-level revenue report must read the stored components, never `componentsOf`.**
- **Serialized selling** (the step 6 obligation): the line names its machines, each costed at its own lot cost with **no FIFO** (brief §5), marked `SOLD`, carrying the buyer and the invoice line — biconditional by CHECK. **No shortfall on this path**: a named machine is refused rather than flagged, because there is no aggregate for it to be negative in.
- **The Credit Note (Q26)** is its own transaction type: it references the invoice it corrects and each line the invoice line it credits, debits the channel's `Sales returns`, **always credits AR even against a cash sale**, is immutable once issued, and **credits the VAT the sale actually charged** rather than re-resolving the rate.
- **A stock return is not a reversal.** Reversal says the consumption should not have happened (whole quantity, once, exact mirror); a return says the goods came back (partial, repeatable, ordinary entry), at **the cost the stock left at**. A credit note that restored stock is **not reversible** — ADR 0008's principle in the other direction.
- **Receipt and Payment share one table.** `SettlementDirection` decides the side and the `JournalSource`; `PartyType` decides the control account. All four combinations are real, refunds included. The trigger to split them is the first column belonging to one and not the other; there is none today.
- **Q13's second half:** editing a settlement below its allocated total releases allocations **most-recent-first**, reducing the last one partially, every release audit-logged. `allocation_order` exists because `created_at` cannot say which of several created in one transaction was last.
- **Q16:** an overpayment becomes a standalone `customer_credit` document that **posts nothing** — the money is already in AR. Created **only when the caller says so**, because "overpaid" and "unmatched remittance" are different facts (rule 7); the unmatched case stays queryable.
- **Q15's remainder:** a rounding difference above `ledger.rounding.threshold` **refuses the document** until somebody accepts it; at or below it, it posts automatically. **The difference always posts**, so AR agrees with the document the customer holds. The acceptance is stored on the record. **No review queue** — a queue is a second copy of state that goes stale. Generalised: visible-at-entry is confirmed at entry; a consequence the operator cannot fix (Q17's shortfall) is flagged and queried.
- **Q10:** "Πελάτης Λιανικής" is seeded with `CustomerSystemKey.RETAIL_WALK_IN` — VAT treatment fixed at `DOMESTIC` by CHECK, undeactivatable by CHECK, refused on both sides of a merge (merge still not built). **Not a default**; nothing falls back to it.
- **The Bank Transfer** is two lines and no third account — brief §4 drops Manager's Inter Account Transfers, which Manager had under Equity.
- **13 new `AccountSystemKey` values** (3 Sales, 3 Sales returns, `Services`, and the six accounts `SettlementMethod` resolves). No key for `Cost of service sold`, because nothing computes a service's cost.

**Landed costs — built (step 10). Q18 answered as ADR 0010; ADR 0011 closes what it introduced.**

- **`Freight / Landed Cost — Unallocated` finally clears.** V4 created it and flagged it `expected_to_clear`, step 8 gave a carrier's invoice somewhere to land, and nothing had ever taken anything out of it. A `FreightAllocation` credits it and debits the goods.
- **A lot's share splits by what is still in the lot** — the whole of Q18. Stock on hand carries its share on the unit cost; stock already gone sends its share to `Landed cost variance`, because those units are not there to carry it and the COGS that took them out is immutable (ADR 0006/0008). **A fully-sold lot may still be named**, and all of its share goes to variance — that is the case Q18 exists for.
- **The received cost is frozen and the carrying cost is derived.** Two columns, no stored total. Allocation divides by the received cost so a second freight invoice splits the same lots in the same proportion as the first; otherwise the answer would depend on data-entry order and nobody would ever notice.
- **An allocation names its source line** — a purchase invoice expense line pointed at the unallocated account — so "how much of this freight is left" has an answer and the account cannot be credited below what was debited into it. One source line per allocation; many lots across many invoices; **lots always named, never inferred** (rule 7). Refused: a zero-cost lot (proportional-by-value gives it no share), a lot from a reversed Goods Receipt, an over-allocation, a currency mismatch.
- **Immutable, reversal refuses once the lots have moved** — ADR 0008's stance. The guard tests the lot's remaining quantity against what it was at allocation time.
- **⚠️ The returns/reversals asymmetry (ADR 0011) is deliberate.** A return catches the freight up in the same entry; a consumption or write-off reversal refuses on a re-costed lot and names a remedy (reverse the allocation, reverse the movement, re-allocate) that a test walks end to end. They differ because a return says the sale was real and a reversal says it never happened — the second means the allocation's *split* was wrong, not just its counterpart.
- **The Inventory control account and lot valuation now provably agree**, which is what found the defect and what proves it fixed.

**Payment settlement automation (brief §6, as built):** Cash→Cash account (hard-blocked >€500, Greek law); POS/Skroutz/ACS→their clearing account (open until real bank remittance); Bank deposit→stays open until Bank Aggregator confirms (chosen: best practice over convenience); Credit→stays open against AR. Real-time balances via frequent (not nightly) Bank Aggregator polling.

**Invoice categorization:** 4 categories (Inventory, Fixed Asset, Expense, Prepaid/Deferred). **Freight allocation is built (step 10)** — proportional by value, the known approximation against weight-based, accepted and unchanged. Automatic categorization itself is still not built: defaults suggested at Product-level then Supplier-level, else "Unclassified — Needs Review", belongs with the myDATA import that creates uncategorised invoices.

**AADE-first invoice import:** default rule — pull from myDATA automatically, confirm against Goods Receipt; missing-from-myDATA is itself a supplier compliance flag.

**Permissions and authentication — built (step 4).**

- **Roles are data, what they grant is code.** `Section` and `ProtectedField` are enums (what exists is determined by what was built); `app_role` / `role_section_grant` / `role_field_restriction` are tables, so creating a role is an operation not a migration — brief §7's "multiple custom roles from the start".
- **Access is default-deny**, so "everything else is invisible" needs no enumeration and stays true as sections are added. **`JOURNAL` is new in step 7** and separate from `CHART_OF_ACCOUNTS` for the reason `INVENTORY` is separate from `PRODUCTS`: seeing the *list* of accounts is close to harmless, while seeing what has posted to them is every financial figure in the business. No grants seeded.
- **Owner and Admin use a `full_access` flag, not stored grants**, so a section added later is visible to them at once. Both are **system roles**: unmodifiable, undeletable, so nobody can strip `USERS_AND_ROLES` from the last role holding it.
- **Remote/Order Staff seeded:** FULL on Sales Order Fulfillment / Customers / Back-in-Stock; VIEW on Products; nothing else. Not a system role, so it stays adjustable. **Field restrictions narrow, never widen.**
- **⚠️ Q21 revised in step 15 (V26): no field is restricted from any role, and that is a decision.** V6 hid `PRODUCT_LAST_PURCHASE_PRICE`, `PRODUCT_SUPPLIER` and `PRODUCT_SUPPLIER_SKU` from Remote/Order Staff; V26 removes all three, because the business has no confidentiality need behind them — a bank balance might reasonably stay hidden from a home-based worker, what a bag of beans cost does not. Since those three values are the only fields the mechanism knows about and that role held the only restrictions, **nothing is field-restricted anywhere and brief §7's inner permission layer is now unused.** **Expressed as deleting data, not code:** the enum keeps its values, the CHECK keeps listing them, and `RoleView.canSee` / `ProductView.redactedFor` / the supplier-implies-SKU narrowing / the three ArchUnit rules are untouched, so restricting one again is an `INSERT`. A future case was named when it was decided — a **bank or partner-clearing balance**.
- **⚠️ Which is why the redaction tests now build their own restricted role.** With no restriction in real data, a change that stopped `ProductService`'s `...For` reads consulting the role would pass everything while removing the guarantee — the audit-log defect's shape again. `ProductIT`, `BundleIT`, `SecurityIT` and `MasterDataEndpointIT` create a role and call `RoleService.restrictField` at runtime instead of relying on the seed. `MasterDataEndpointIT` asserts against the bytes that staff and owner now receive the **same** product on both the single read and the list.
- ~~**Step 5 obligation**~~ — **done.** `ProductView.redactedFor(RoleView)` applies all three when a role restricts them; unused by seeded data since V26, and still proven by runtime-restricted roles.
- **Auth: server-side sessions, HttpOnly cookie** (Q22, approved). `SameSite=Strict`, `Secure`, 8h, new session id on login, CSRF on. Login/logout return 204/401 not redirects, and `/api/**` returns 401 not a redirect — a `fetch()` cannot use a 302 to an HTML login page. No login controller was written; Spring Security's own `/login` and `/logout` are used.
- **Password hashes never leave the core.** `UserService.authenticate(username, rawPassword)` verifies internally; a custom `AuthenticationProvider` in `app` calls it, and the session principal's `getPassword()` returns null. Login failures are indistinguishable (unknown user, wrong password, inactive user, inactive role) with the reason recorded only in the audit log.
- **No seeded account, no default password.** The first Owner comes from `NOVOCORE_BOOTSTRAP_OWNER_USERNAME`/`_PASSWORD` and **the app refuses to start** if the user table is empty and they are unset. **That refusal fired for real in step 11** — the Compose stack had never had an owner, so it crash-looped until one was supplied, which is the mechanism working as designed. **The first Owner, `kostas`, now exists** (created 2026-07-28, randomly generated password). ⚠️ **It is no longer in `docker/.env`** — that file now carries only `NOVOCORE_DB_PASSWORD`, `NOVOCORE_SITE_ADDRESS` and `NOVOCORE_BACKUP_ENCRYPTION_KEY`, and the bootstrap variables were removed after first use exactly as `compose.yml`'s comment says is safe. **So the password lives wherever the owner put it and nowhere in this repo**, which is correct but has a consequence worth knowing in advance: *anything that has to drive the live stack over HTTP needs the owner to run it.* S1's search check hit this — its database half was proven directly, its HTTP half was left to the owner as two URLs. **Login has now been exercised end to end against the running stack over HTTPS** — 401 unauthenticated, 204 on login with a CSRF token, 200 returning the chart of accounts — so Caddy, TLS, the session cookie, CSRF and `requireView` are proven together against real containers rather than only in a test. **Still true: no human has used a browser, and the frontend has no login screen.** There is also **no change-password screen**, so changing that password today means `UserService` or a direct hash update.
- **Password policy settled (Q29):** 12 characters minimum, no composition rules, per NIST SP 800-63B. **2FA decided as no-for-now with a condition (Q30) — now pre-launch blocker PLB-1, above.** ~~**Q31**~~ **is confirmed in step 9: single role per user stays, no change needed.** Still open: the 8-hour session timeout (Q32).

**REST surface — built in step 14: 133 routes across 11 controller packages** (`423bf34`, `e6354d6`, `b8aa9e2`, `f2e8e06`), replacing the single read-only endpoint step 4b left. Split into three committed sub-steps: foundations + master data, purchasing + inventory, sales + settlements + outbox.

- **Authorisation is a typed `@Requires(section, level)` plus an interceptor**, replacing step 4b's inline `requireView`. The failure an annotation introduces is that it can be *forgotten*, which fails open silently — so **three layers refuse an undeclared `/api/**` handler**: an ArchUnit rule fails the build, a startup check refuses to complete the context refresh, and the interceptor refuses the request. All three proven to fire.
- **Money crosses the wire as a string** — `{"amount": "12.50", "currency": "EUR"}`, quantities bare strings — because a JSON number becomes an IEEE-754 double in a browser, breaking rule 5 at the one outward-facing layer. **A JSON number is refused rather than rounded.** Tested against **12.505**, the cost behind Q45.
- **Error mapping is complete**: 404/422/409/410/413/503/400, with a deliberate asymmetry — permission refusals stay generic, validation refusals carry the core's own message. A test enumerates `core-api`'s exceptions so none can fall through to a 500 unnoticed.
- **Commands, not CRUD.** One route per named service operation; no whole-object `PUT`. Lists are wrapped in `{"items": […]}` and **step 14 ships unpaged**, deliberately.
- **⚠️ Six `InventoryService` methods have no route and an ArchUnit rule forbids one** — `receive`, `unreceive`, `consume`, `reverseConsumption`, `applyLandedCost`, `removeLandedCost`. Each moves stock and posts nothing on its own. **Stock moves through documents**, or the Inventory control account stops agreeing with what the lots carry.
- **`GET /api/products/{id}/stock` is under `PRODUCTS`, not `INVENTORY`** — quantities carry no cost, and an order picker needs them. **An asset's carrying value gets no route**, because it is a `JOURNAL` read.
- ~~**Not built:** users, roles, settings, audit log, backup administration, journal writing, VAT-class administration.~~ **Step 16b built users, roles, settings, the journal *listing* and VAT-class/unit-of-measure administration** — 174 routes now. **Still not built, each deliberately:** the **audit log** (no route, and it is the one read that would expose every other section's activity at once); **backup administration** (its whole namespace is excluded from Settings, and it is its own step with its own section decision); and **journal *writing*** — `postManualEntry`, `amend` and `reverse` all have services and no routes, because a manual-entry screen is a design conversation rather than something to add behind a listing.
- **⚠️ No human has used a browser.** Every route is exercised by an integration test over the real filter chain and none by a person; the frontend still has no login screen.

**✅ Q44 — answered in full (step 14c).** The **section half** is a new `Section.EMAIL_OUTBOX`, deliberately not folded into `SETTINGS`: changing the SMTP password and reading who was emailed about what are different grants. The **access-path half** is built — `downloadAttachment` takes the viewer as a **required parameter with no unchecked overload**, and re-checks a referenced attachment against the section governing the record it belongs to. **It needed a piece the decision could not have anticipated**: `AttachmentService.entityType` is free text, so there was nothing to check against — hence **`AttachmentOwnerType`**, a typed registry that is **fail-closed on an unknown type, denying even the Owner** (if only restricted roles were refused, a missing registration would be invisible to whoever could fix it). **Proven behaviourally**: both denial tests were confirmed to fail against the check removed, then it was restored — the step-12 audit-log lesson applied on purpose.

**🐛 Step 14's plan said "no migrations expected" and was wrong.** `role_section_grant` carries a CHECK listing every section by name, so `EMAIL_OUTBOX` existing only in Java **could not be granted at all**. **V25** adds it, and `SecurityIT` now holds the CHECK to the enum in both directions plus a test that a grant can really be stored for every section — so the next person adding one finds out from a named test rather than from three unrelated failures. **Any new `Section` is a migration.**

**Three more things step 14 turned up:** `ProductService.allFor(viewer)` returned `active()`, contradicting its name and leaving `all()` with no redacted counterpart (fixed, plus the four missing `For` variants); `EndpointDeclarationCheck` died at startup because a real context has **two** `RequestMappingHandlerMapping` beans, MVC's and the actuator's (it now checks every mapping); and `BundleService`'s two `ProductView` reads were outside the redaction rule — correct in behaviour, conventional in guarantee — now closed with their own `For` variants and the rule extended.

**Barcode scanning:** not a module — an input mechanism across Purchase Invoice/Goods Receipt verification, picking, in-store sales, and a new barcode-first entry point into Product Creator (falls back to supplier link, then manual). Once Product Creator exists, NovoCore becomes the point of product creation, syncing outward to Go/Woo — not the reverse.

## Adapters (see brief §8 for full detail)

Prosvasis Go (transitional), WooCommerce, ACS Courier (clearing + voucher generation), Skroutz (clearing + receiving their voucher), POS provider/ePay, POS terminal (deferred), Bank aggregator, File import (kept — clearing reconciliation + migration), AADE myDATA, AADE Provider/Πάροχος, AADE/VIES lookup. Dropped: invoice OCR.

## Modules (see brief §9 for full detail)

Purchase Orders, Sales Order Fulfillment (status list: Processing/On Hold/Completed/Cancelled/Pending Payment/Failed/Draft/Refunded; QZ Tray for printing), Price Tag Printing, AI Analysis (last, read-only), Product Creator, Reports, Clearing Checks, Roast Date Report, Back-in-Stock Reminders, Service/Technician Management, Accountant Monthly Package, Employee Digital Work-Card/Ergani (tentative, needs accountant).

## Roadmap phase order (proposed)

0. Setup (done) → 1. Core → 2a. Dummy data validation → 3. WooCommerce + Go adapters → 2b. Real migration + parallel-run with Manager → 4. Purchase Orders + Sales Order Fulfillment → 5. File import → 6. Bank aggregator → 7. myDATA + VIES → 8. Reports + Clearing Checks → 9. smaller modules → 10. AI Analysis → 11. Core-owned invoice issuing + AADE Provider + POS terminal (retires Go).

## Explicitly still open

**`docs/PROGRESS.md` carries the full numbered list with which build step each one blocks.** The
ones that will stop work soonest:

- ~~**ChargeType income account (Q27)**~~, ~~**the VatExemptionReason seed**~~, ~~**Q5**~~, ~~**Q6**~~, ~~**Q8**~~, ~~**Q9**~~, ~~**Q12**~~ — **all resolved and built** (V7, V8, V9). See the sections above.
- ~~**Q33**~~ (fee VAT independent of the goods, confirmed with the accountant) and ~~**Q34**~~ (unit of measure as a table) — **both answered; Q34 built in V11.**
- ~~**Q7**~~ (stock per location plus a computed sellable figure), ~~**Q25**~~ (write-off reason as a fixed enum) and ~~**Q11**~~ (bundles built now) — **all three answered and built in step 6.** See the Inventory and Bundles sections above.
- ~~**Q13 and Q14**~~ — **both answered and built in step 7**, as ADRs 0006 and 0007. So are ~~**Q19**~~, ~~**Q26**~~, ~~**Q15**~~ and ~~**Q16**~~ — **and all of their implementations landed in step 9**, along with Q13's second half.
- ~~**ADR 0004's open item**~~, ~~**Q17**~~ and ~~**Q39**~~ — **all three answered and built in step 8**, as ADR 0008. See above.
- ~~**Step 9's seven obligations**~~ — **all discharged** (ADR 0009): Q13's second half, the VAT dimension on invoice postings, the two-entry sale, serialized consumption, `BundleService.dissolve` after a sale, Q15's remainder, and Q16/Q26. ~~**Q10**~~ and ~~**Q31**~~ answered with them.
- ~~**Step 10 / Q18**~~ — **answered and built** as ADR 0010, with ADR 0011 closing the defect it introduced.
- ~~**SMTP credentials, and where they live**~~ — **answered and built in step 11.** All email configuration lives in **Settings**, not the environment: the exposure argument for the environment was that Settings sits inside the backup step 12 copies to Drive, and that does not apply because Drive access is scoped to one person. **The password is nonetheless absent from every migration** — that is a separate question from where it lives, since a migration is a file in git.
- **Needs the accountant, all three NULL and fail-loud until then:** statutory **depreciation rates** per asset category plus the category taxonomy (**do not create real assets with real values until confirmed**), the **AADE exemption codes 24 and 28** plus the OSS/IOSS myDATA codes (Q35/Q36), and the **myDATA unit-of-measure codes** (Q38).
- **Q37** — Customer and Supplier have no address fields (not needed while Go issues invoices, needed by phase 11), no human-facing codes, and only one selling price per product.
- **Dispatch purpose / Σκοπός διακίνησης (Q28)** — recommendation: a **core-owned `GoodsDispatch`** (outbound counterpart to Goods Receipt) with a `DispatchPurpose` lookup, in **roadmap phase 4**; not inside the Sales Order Fulfillment module, since supplier returns, inter-location transfers and repairs are dispatches too. Conditional on two unknowns: whether Go already issues Δελτία Αποστολής (if so this is phase 11), and whether the AADE digital delivery note regime applies (accountant question).
- ~~**VAT class list (Q4)**~~ — **resolved and built**, see the VAT section above.
- ~~**VAT posting mechanics (Q14)**~~ and ~~**correction/reversal policy (Q13)**~~ — **both resolved and built**, see the ADRs above. Both obligations they left into step 9 are **discharged**: the sales invoice postings supply the VAT dimension per line and summed by class, and Q13's second half is `SettlementService.amend`, releasing allocations most-recent-first with every release audit-logged.
- ~~**Remote Staff restricted fields + auth mechanism (Q21, Q22)**~~ — **both answered and built**, see the permissions section above. Left over from Q22: **2FA (Q30) and password policy (Q29)** are open, and worth deciding before the system faces the internet rather than after.
- ~~**Bundle/Composite products (Q11)**~~ — **built in step 6.** See the Bundles section above.
- ~~**Landed cost after consumption (Q18)**~~ — **answered and built in step 10 (ADR 0010)**, within ADR 0008's constraint. See the Landed costs section above. ~~The last-purchase-price obligation~~ is discharged with it.
- **Q42** *(new)* — **a bundle containing a serial-tracked component cannot be sold as a bundle.** Step 9 refuses it, naming why: which machine left the shelf is a fact somebody scanned, and a bundle line names no serial numbers. The machine sells on its own line today. Not blocking anything.
- ~~**Q45**~~ — **answered and fixed the same day it was raised, as ADR 0015 plus migration V24.** See the ADR list above.
- **Q41** — **after-the-fact GR/IR matching.** Nothing matches an existing invoice to an existing delivery later; it needs an answer to "whose document is the variance entry?" and belongs with phase 8's Clearing Checks.
- ~~**Write-off reason field (step 3 obligation, + Q25)**~~ — **fully discharged in step 7.** The enum (`SHRINKAGE`/`DAMAGE`/`EXPIRY`/`OTHER`) landed in step 6 with no consumer; `stock_write_off` now carries it.
- **Damaged Goods aging check (step 3 obligation)** — still phase 8's to write, but **step 6 built the query it reads** (`lotsAt` / `unitsAt`), covering both shapes of lot.
- ~~**Negative stock (Q17)**~~ — **answered and built in step 8** (ADR 0008): allowed in aggregate, flagged, never retro-costed. A single lot still cannot go below zero, by CHECK.
- ~~**Credit note as a typed transaction (Q26)**~~ — **answered and built in step 9**: its own type, referencing the invoice it corrects, posting to the per-channel `Sales returns` account, immutable once issued, crediting the VAT the sale actually charged.
- **Q40** — does a journal entry need a **human-facing entry number**? The id is the handle today; a numbering scheme carries a format decision nobody has been asked, so none was guessed. **Step 8 widened it** to purchase invoices and goods receipts. **Step 9 does not narrow it**: sales invoices and credit notes carry a number, but it is the *issuing system's* (Go's), not one NovoCore assigns — so the question stands unchanged for the documents NovoCore owns.
- ~~**Q24**~~ — **answered and built in step 12 (ADR 0013).** Drive API (not `rclone`), OAuth refresh token per account, AES-256-GCM at rest, retention **7 rolling + every month's last forever**. **⚠️ What remains is not a decision but an action: the two Drive folder ids and OAuth credentials, which have never been recorded anywhere.** Until the consent flow is done per account, both destinations record `NOT_CONFIGURED` every run and backups are **local only** — visible, but not a backup regime.
- ~~**Q43**~~ — **answered and built (V22).** Referencing split it in two and the halves took different numbers: **message rows `FOREVER`** (cheap since V21 — a year of history is rows, not megabytes) and **inline copies of generated attachments 90 days** (PO PDFs and reports, the only unbounded growth left). Both in **Settings**. `EmailRetention` runs daily, scheduling enabled in `app` so the core's tests call `pruneNow()` and nothing sleeps. **The guards are the substance, not the `UPDATE`:** `content_source = 'INLINE'` — a **referenced document's bytes are never pruned here**, they belong to `AttachmentService`, and widening this would make one service delete another's documents; `status = 'SENT'` — a PENDING message still needs its bytes and a **FAILED one keeps them because retry is why it was kept**; `content IS NOT NULL` for idempotence. **An unreadable setting stops the prune and deletes nothing** — the one setting here with no safe default, so `FOREVER` is a word rather than a blank. Row deletion is built although it never runs, so the setting is not decorative.
- ~~**Q44**~~ — **both halves answered and built in step 14c.** `Section.EMAIL_OUTBOX` is its own grant, not part of `SETTINGS`; the access-path re-check lives in `EmailSenderImpl` and is proven behaviourally. It needed `AttachmentOwnerType` — a typed, fail-closed owner-type-to-`Section` registry — because `AttachmentService.entityType` is free text and there was nothing to check against. See the REST surface section above.

Also still open from the brief: bank aggregator selection, POS provider (epay vs NBG), invoice/template design mechanism, **backup restore test (nothing exists yet)**, physical hosting machine. (Freight allocation is no longer on this list — proportional-by-value is built as the brief specified.) Needs accountant: AADE Πάροχος scope, Ergani applicability, AADE cash-register/POS interconnection mandate.

## Naming

Product: **NovoCore**. Company name (if commercialized) undecided.

---

*Business context: Novotrade S.A., trading as Java Jives, a coffee equipment retailer in Greece (javajives.gr, WooCommerce). Owner is not a developer and won't read code directly — relies on Claude Code for implementation and this kind of primer for design continuity. Prefers direct, fact-based feedback over agreement, and follows established best practice as the default tie-breaker when genuinely unsure.*
